# MIPS汇编

## 简介

> MIPS（Microprocessor without Interlocked Pipeline Stages）是一种精简指令集计算机（RISC）架构。MIPS汇编语言是一种低级编程语言，用于直接与MIPS处理器进行交互。本文将介绍MIPS汇编的基本概念、指令格式、寄存器使用以及常用指令。

**目录**

1. [数据的表示与存储](#数据的表示与存储)
2. [寄存器简介](#寄存器简介)
3. [地址与内存](#地址与内存)
4. [初识指令](#初识指令)
5. [运算类指令](#运算类指令)
6. [系统调用与伪指令](#系统调用与伪指令)
7. [汇编指示（Directives）](#汇编指示directives)
8. [指令的书写格式](#指令的书写格式)
9. [标签](#标签)
10. [立即数](#立即数)
11. [认识机器码](#认识机器码)
12. [指令格式](#指令格式)
13. [结合手册读懂指令](#结合手册读懂指令)
14. [立即数的处理方式](#立即数的处理方式)
15. [寻址方式](#寻址方式)
16. [访存类指令](#访存类指令)
17. [跳转类指令](#跳转类指令)
18. [编程实战：从 C 到 MIPS](#编程实战从-c-到-mips)
19. [程序跟踪例题](#程序跟踪例题)

> **学习提示**：MIPS 的学习大致分两个阶段——前期重点是「用 MIPS 编程」（把 C 程序翻译成汇编，见「编程实战」一章），后期重点是「理解指令集与机器码」（为搭建 CPU 和期末理论考试做准备）。

## 数据的表示与存储

### 数据单位

| 单位 | 大小 | 说明 |
| --- | --- | --- |
| 位（bit） | 1 bit | 最小存储单位，值为 0 或 1 |
| 字节（Byte） | 8 bit | 基本存储单位 |
| 半字（Half-Word） | 16 bit（2 Byte） | 字长的一半 |
| 字（Word） | 32 bit（4 Byte） | CPU 一次处理的数据宽度 |
| 双字（Double-Word） | 64 bit（8 Byte） | 两个字，用于双精度浮点数等 |

在 32 位 MIPS 中，「字」是最常用的单位：`lw`/`sw` 按字访问，`lh`/`sh` 按半字访问，`lb`/`sb` 按字节访问。

**常用换算与进制**：

- 一个 32 位寄存器 = 4 字节 = **2 半字或 1 字**。
- **字节是地址的基本单位**：内存中每个地址对应的空间是 1 字节（8 位），既不是 1 位，也不是 32 位。
- 二进制数 `0b10101` 是 5 位；十六进制的**每一位数字对应 4 位二进制**（如 `0xa` = `0b1010`），所以 `0xabc` 共 12 位。

**换算小例**：`int a[10]`（10 个元素）占 10 × 4 = **40 字节** = **10 字** = **20 半字**；把 `0x12345678` 从地址 `0x1000` 开始存放，被占用的地址是 `0x1000` ~ `0x1003`。

> **对齐（Alignment）**：访存指令要求地址对齐——按字访问的地址必须是 4 的倍数，按半字访问的地址必须是 2 的倍数，否则 MARS 会报「address not aligned」异常。

### 大端存储与小端存储

**字节序（Endianness）** 描述多字节数据（如 16 位、32 位整数）在内存中按什么顺序存放：

- **大端（Big-endian）**：最高有效字节（MSB）放在**最低内存地址**；
- **小端（Little-endian）**：最低有效字节（LSB）放在**最低内存地址**。

以 32 位整数 `0x12345678` 为例，其中 `0x12` 是最高有效字节，`0x78` 是最低有效字节。假设从内存地址 `0x1000` 开始存放：

| 内存地址 | 大端存储 | 小端存储 |
| --- | --- | --- |
| `0x1000` | `0x12`（高字节） | `0x78`（低字节） |
| `0x1001` | `0x34` | `0x56` |
| `0x1002` | `0x56` | `0x34` |
| `0x1003` | `0x78`（低字节） | `0x12`（高字节） |

要点：

- 记忆方法：大端与书写顺序一致（高字节在前），小端则相反（低字节在前）。
- 只要按同一宽度整体读写，两种字节序得到的数值相同；只有在按字节访问、或跨机器传输数据时才需关心字节序。
- MIPS 架构本身不规定字节序，允许大端或小端实现；本课程使用的 MARS 模拟器采用**小端**存储。
- 验证方法：用 `.word 0x12345678` 定义数据，再用 `lb` 取出第一个字节——得到 `0x78` 说明是小端，得到 `0x12` 则是大端。

## 寄存器简介

### 什么是寄存器？

> 寄存器是 CPU 的组成部分之一，是一种高速存储器（甚至是 CPU 可以使用的最高速的一种存储器），可以用来暂存指令，数据，地址等。

### MIPS寄存器

> MIPS架构中有32个通用寄存器，每个寄存器都是32位的。寄存器编号从0到31，通常使用符号名称来表示。以下是常用的MIPS寄存器及其用途：

| registers | Name | usage |
| --- | --- | --- |
| `$0` | `$zero` | 常量0 |
| `$1` | `$at` | 保留给汇编器使用的临时变量 |
| `$2-$3` | `$v0-$v1` | 函数调用返回值 |
| `$4-$7` | `$a0-$a3` | 函数调用参数 |
| `$8-$15` | `$t0-$t7` | 临时变量 |
| `$16-$23` | `$s0-$s7` | 需要保存的变量 |
| `$24-$25` | `$t8-$t9` | 临时变量 |
| `$26-$27` | `$k0-$k1` | 留给操作系统使用 |
| `$28` | `$gp` | 全局指针 |
| `$29` | `$sp` | 堆栈指针 |
| `$30` | `$fp` | 帧指针 |
| `$31` | `$ra` | 返回地址 |

特别地，对于 `$0` 和 `$1` 两个寄存器：

- `$0` 一般不能用于赋值，但这不代表你对它赋值就是错的。在实际中，你可以对 `$0` 进行赋值，但它的值会始终保持为 0。也就是说对 `$0` 赋值并不违反语法，但没有实际效果。
- `$1` 保留给汇编器，一般不使用此寄存器(使用该寄存器可能会导致难以发现的 bug，具体原因请在编写 MIPS 汇编时注意扩展指令是如何翻译成普通指令的)

其余几点容易踩坑的约定：

- **32 个通用寄存器合称寄存器堆 GRF**（General Register File）；每个寄存器存 32 位值，**大小和 C 语言常用标准中 `int` 类型相同**。
- 编号（本名，如 `$16`）与代号（如 `$s0`）**完全等价**，在汇编代码中写哪一个都可以。
- **`$26` ~ `$31` 各有特殊用途**（`$k0`/`$k1` 留给操作系统，`$gp`/`$sp`/`$fp`/`$ra` 各有分工），**不要拿来当普通变量使用**。
- **`$v0` 和 `$a0` 会用于 `syscall` 的参数传递**，建议不要随意「借用」，让它们各司其职。

**小例**：`addi $16, $0, 1` 与 `addi $s0, $0, 1` 是同一条指令的两种写法（`$16` 就是 `$s0`）；而执行 `addi $0, $0, 5` 之后，`$0` **仍然是 0**，不会变成 5。

### 寄存器使用约定

实际写程序时，寄存器的选择有约定俗成的习惯：

- **临时数据/地址**：优先使用 `$t0-$t9`。
- **需跨函数调用保存的变量**：使用 `$s0-$s7`。
- **函数参数**：前 4 个用 `$a0-$a3`，更多参数通过栈传递。
- **函数返回值**：整数用 `$v0-$v1`，浮点数用 `$f0`/`$f2`。
- **乘除法结果**：结果在 `HI`/`LO`，需用 `mfhi`/`mflo` 移出。
- **浮点数据**：使用 `$f0-$f31`，注意配对使用和保存规则。
- **局部变量**：寄存器不够用或需要取地址时，保存在**栈**上，通过 `$sp` 和 `$fp` 访问。

**函数参数传递**的完整约定：前 4 个（整数或指针）参数依次放入 `$a0`、`$a1`、`$a2`、`$a3`；第 5 个及以后的参数压入**栈**，通过栈指针 `$sp`（以及帧指针 `$fp`）访问。例如传入 3 个整型参数时，依次放在 `$a0`、`$a1`、`$a2`；传入 10 个参数时，第 1～4 个放在 `$a0-$a3`，第 5～10 个压栈。

如果参数是浮点数，则通常使用 `$f12`、`$f14`、`$f16` 等浮点参数寄存器（从 `$f12` 起依次使用）。

### 寄存器选用：君子协定与互相「借用」

表格里说 `$s0`~`$s7` 一般用于存储定值、`$t0`~`$t7` 一般用于存储变量，但这**只是君子协定**，即使不遵守也没有问题，只不过可读性与美观性会受影响。

- t 系列**用完**了，可以去 s 系列「借」几个来用；
- a 系列（**除不太好惹的 `$a0` 之外**）同样可以拿来随便用；三个系列互相借来借去在语法上都没问题。

> 不过并不建议随便借来借去：寄存器本来就无法像变量那样命名，借多了很快就会分不清谁是谁。

### 三个特殊寄存器

> PC（Program Counter）：它用于存储当前 CPU 正在执行的指令在内存中的地址。需要注意的是，这个寄存器的值不能用常规的指令进行取值和赋值，但这并不意味着不能做到，只是麻烦一些。那么，怎样对其取值、赋值呢？这可以作为一个思考的题目，在之后的学习中自行探索。（提示：跳转）
> HI：这个寄存器用于乘除法。它被用来存放每次乘法结果的高 32 位，也被用来存放除法结果的余数。
>
> LO：HI 的孪生兄弟。它被用来存放每次乘法结果的低 32 位，也被用来存放除法结果的商。

`HI`/`LO` 是特殊寄存器，不能用普通指令直接读取或写入，需用 `mfhi`/`mflo` 把结果移出到通用寄存器：

```asm
mult $s0, $s1    # $s0 × $s1 → 64 位结果：HI 存高 32 位，LO 存低 32 位
mflo $t0         # $t0 ← LO（乘积低 32 位）
mfhi $t1         # $t1 ← HI（乘积高 32 位）

div  $s2, $s3    # $s2 ÷ $s3
mflo $t2         # $t2 ← LO（商）
mfhi $t3         # $t3 ← HI（余数）
```

### 寄存器的初始值（以 MARS 初始界面为准）

MARS 刚打开、程序还没开始执行时，右侧**寄存器面板**显示的就是各寄存器的初值：

| 寄存器 | MARS 初始值 | 说明 |
| --- | --- | --- |
| `$0` | `0x00000000` | 恒为 0，任何时候读都是 0 |
| 其余通用寄存器（`$1`~`$27`、`$30`、`$31`，即除 `$sp`、`$gp` 之外） | `0x00000000` | `$v0`、`$a0`、`$t0`、`$s0`、`$k0`/`$k1`、`$fp`、`$ra` 等全部显示为 0 |
| `$gp`（`$28`） | `0x10008000` | 全局数据指针，位于数据段基址下方 32 KB，便于用 16 位偏移访问数据段开头 |
| `$sp`（`$29`） | `0x7fffeffc` | 栈指针，指向栈段的高端（栈向低地址方向增长） |
| `PC` | `0x00400000` | 代码段第一条指令的地址，程序从这里开始执行 |
| `HI` / `LO` | `0x00000000` | 未执行乘除法前都是 0；执行 `mult`/`div` 后分别存放高 32 位（余数）与低 32 位（商） |
| `$f0` ~ `$f31` | `0` | 浮点寄存器面板中同样显示为 0，未写入前不可依赖 |

> **两点提醒**
> - 表中地址是 MARS **默认内存配置**下的取值；若在 `Settings` 中改过内存配置，或换成别的模拟器、真实硬件，数值可能不同——**以 MARS 初始界面为准**。
> - `$t0`、`$a0` 等在 MARS 里虽然是 0，但 MIPS **架构并不保证**这一点。依赖未初始化寄存器的代码换个环境就会出错，安全写法是**用之前先 `li`/`move` 赋值**。

**验证小例**：写一个空程序直接运行，寄存器面板的值与上表一致（点工具栏的 **reset** 可恢复到初值）；也可以自己把它们打印出来看：

```asm
li   $v0, 34       # 34 号系统调用：以十六进制打印整数
move $a0, $sp
syscall            # 输出 $sp 的初值：0x7fffeffc

li   $v0, 11       # 11 号系统调用：打印字符
li   $a0, '\n'
syscall            # 换行

li   $v0, 34
move $a0, $gp
syscall            # 输出 $gp 的初值：0x10008000
```

### CP0 寄存器

> CP0 是一个系统控制协处理器，而 CP0 寄存器则是该协处理器工作时需要用到的一些寄存器。在我们的实验中，只会用到其中的 4 个寄存器：SR、Cause、EPC 和 PRId。
>
> - SR：用于系统控制，决定是否允许异常和中断
> - Cause：记录异常和中断的类型
> - EPC：保存异常或中断发生时的 PC 值，也就是发送异常或中断时 CPU 正在执行的那条指令的地址。当处理完成之后，CPU 会根据这个地址返回到正常程序中继续往下执行。
> - PRId：处理器 ID，用于实现个性的寄存器。

**小例**：执行某条 `addi` 时发生溢出异常，CPU 会把**出错指令的地址**存入 `EPC`，然后跳到异常处理程序；处理完后用 `eret` 依据 `EPC` 的值返回到出错处继续执行。

## 地址与内存

寄存器只有 32 个，装不下 `int[32]` 数组这样的数据，所以 MIPS 另有一片**庞大的、连续的内存空间**用来存放数据（内存之外还有更大的外存空间，属于理论课内容）。

- 内存太大，不可能像寄存器那样给每一块 32 位的空间都起名字；但因为它**连续**，可以像数组下标一样**从 0 开始为每块空间编号**，这个编号就是**内存地址**。
- **每个地址代表的空间是 8 位（1 字节）**，既不是 1 位，也不是 32 位——这就是「字节是地址的基本单位」的含义。

**例：把 32 位数据 `0x12345678` 写入地址 `0x0`**

| 问题 | 答案 | 理由 |
| --- | --- | --- |
| 下一个还没被写入数据的地址是？ | `0x4` | `0x12345678` 有 32 位（4 字节），占据了 `0x0`、`0x1`、`0x2`、`0x3` 四个地址的空间 |
| `0x0` ~ `0x3` 这 4 个地址里分别是什么？ | `0x78`、`0x56`、`0x34`、`0x12` | 一个数字的**最右侧才是最低位**，按从低位到高位的顺序每次取 8 位，最先取到的必然是 `0x78`（即小端存储） |

**地址的构造方式**：`imm16($t0)` 表示「`$t0` 中的值 + 立即数」这个地址，**立即数可以是负数**。

```asm
# 设 $t1 = 0x12345678、$t2 = 0x00000007
sw $t1, 1($t2)     # 写入地址 0x7 + 1 = 0x8
sw $t1, -3($t2)    # 写入地址 0x7 - 3 = 0x4（立即数为负）
```

> 地址部分看起来简单，却几乎决定了后面的访存指令、CPU 搭建时的地址变换、期末理论考试的计算题乃至操作系统内存管理能否学得下去，务必「大彻大悟」。

## 初识指令

**指令**是处理器指令集架构（ISA，即计算机体系结构中与程序相关的部分）定义的一次独立操作，一般是运算、存储、读取等。它在 CPU 中的真实存在形式是高低电平，即由 01 序列组成的机器码。机器码人类难以阅读，所以指令一般用汇编语言来表示，也就是汇编指令——汇编指令只是指令的一种表示形式，本质与机器码相同。

一条指令的功能较为单一，一般不具有复杂的逻辑，例如「将某两个寄存器的值相加并存入另一个寄存器」「若某个寄存器的值满足条件则跳转至某条指令」。但用这些简单指令，最终可以组合出功能强大的程序。

一段汇编程序示例：

```asm
.data
.text
.global main
main:
addi $t0, $0, 100 # tag
ori $t1, $0, 200
add $t2, $t1, $t2
sub $t3, $t2, $t1
lui $t4, 233
ori $v0, 1
ori $a0, 2333
mthi $t1
ori $a0, 2333
mthi $t1
syscall
nop
loop:
j loop
nop
```

其中被 `tag` 标记的 `addi $t0, $0, 100` 就是一条指令，含义为「将 `$0` 寄存器的值加上 100，并将结果存入 `$t0` 寄存器」。

**注意**：汇编语言并非全部由指令组成。上面的代码中，第 5–16、18–19 行是严格意义上的指令，其余为标签、伪指令等。

### 指令助记符全称对照

助记符（mnemonic）都是英文单词的缩写，看懂缩写规律后，大部分指令名称都能「望文生义」。

### 命名规律

| 组成部分 | 含义 | 例子 |
| --- | --- | --- |
| 后缀 `i` | **I**mmediate，立即数版本 | `addi` 即 `add` + i |
| 后缀 `u` | **U**nsigned，无符号版本 | `addu`、`sltu`、`addiu` |
| 后缀 `v` | **V**ariable，移位量放在寄存器中 | `sllv`、`srlv`、`srav` |
| 前缀 `l` / `s` | **L**oad / **S**tore，读 / 写内存 | `lw`、`sw` |
| 后缀 `b` / `h` / `w` | **B**yte / **H**alfword / **W**ord，字节 / 半字 / 字 | `lb`、`lh`、`lw` |
| 末尾 `z` | **Z**ero，与 0 比较 | `blez`、`bgtz`、`beqz` |
| 开头 `b` | **B**ranch，条件分支 | `beq`、`bne`、`bgtz` |
| 开头 `j` | **J**ump，无条件跳转 | `j`、`jal`、`jr` |

> 例：`sltiu` = **S**et on **L**ess **T**han **I**mmediate **U**nsigned，即「立即数版、按无符号比较的小于则置 1」。

## 运算类指令

按「位运算 → 加减法 → 乘除法 → 移位 → 立即数运算」的顺序，逐个介绍最常用的运算类指令。

### 算术与逻辑（R 型）

| 指令 | 全称 | 含义 |
| --- | --- | --- |
| `add` | **ADD** | 加法 |
| `addu` | **ADD** **U**nsigned | 加法（不检查溢出） |
| `sub` | **SUB**tract | 减法 |
| `subu` | **SUB**tract **U**nsigned | 减法（不检查溢出） |
| `and` | **AND** | 按位与 |
| `or` | **OR** | 按位或 |
| `xor` | e**X**clusive **OR** | 按位异或 |
| `nor` | **N**ot **OR** | 按位或非 |
| `slt` | **S**et on **L**ess **T**han | 小于则置 1（有符号） |
| `sltu` | **S**et on **L**ess **T**han **U**nsigned | 小于则置 1（无符号） |
| `mult` | **MULT**iply | 有符号乘法，结果入 `HI`/`LO` |
| `multu` | **MULT**iply **U**nsigned | 无符号乘法 |
| `div` | **DIV**ide | 有符号除法 |
| `divu` | **DIV**ide **U**nsigned | 无符号除法 |
| `mfhi` | **M**ove **F**rom **HI** | 从 `HI` 取出 |
| `mflo` | **M**ove **F**rom **LO** | 从 `LO` 取出 |
| `mthi` | **M**ove **T**o **HI** | 写入 `HI` |
| `mtlo` | **M**ove **T**o **LO** | 写入 `LO` |

**小例**：逐位与 / 或 / 异或的结果可以直接按位看出来

```asm
li  $t0, 0x0f0f0f0f
li  $t1, 0x00ff00ff
and $t2, $t0, $t1    # $t2 = 0x000f000f（两边都是 1 才为 1）
or  $t3, $t0, $t1    # $t3 = 0x0fff0fff（有一个 1 就为 1）
xor $t4, $t0, $t1    # $t4 = 0x0ff00ff0（两位不同才为 1）
nor $t5, $t0, $0     # $t5 = 0xf0f0f0f0（与 0 或非 = 逐位取反）
```

### 位运算的写法要点

| 要点 | 说明 |
| --- | --- |
| 结果写在**最前面** | `and $t2, $t0, $t1` 表示 `$t2 = $t0 & $t1`，被赋值的寄存器打头，**不是**写在最后面 |
| 立即数位置固定 | `andi $t2, $t0, 15` 中立即数**必须**写在第三个位置，**不可以**和前面的寄存器调换 |
| 立即数可写十六进制 | `15` 与 `0xf` 等价，两种写法都可以 |
| 三寄存器格式统一 | `xxx $t2, $t0, $t1` 一律是「被赋值的寄存器打头，参与运算的两个寄存器殿后」 |

```asm
and  $t2, $t0, $t1   # $t2 = $t0 & $t1
or   $t2, $t0, $t1   # $t2 = $t0 | $t1
xor  $t2, $t0, $t1   # $t2 = $t0 ^ $t1
andi $t2, $t0, 15    # $t2 = $t0 & 15（立即数在第三位）
ori  $t2, $t0, 0xf   # $t2 = $t0 | 15
xori $t2, $t0, 15    # $t2 = $t0 ^ 15
nor  $t0, $t0, $0    # $t0 原地取反（$0 恒为 0）
```

**取反要「略施小计」**：MIPS 指令集并没有直接给出取反指令，但可以用或非指令实现——`nor $t0, $t0, $0` 就能把 `$t0` **原地取反**（借助 `$0` 恒为 0）。

> **不要脑补不存在的指令**：没有 `nori`；也**没有「两个立即数直接运算」的指令**（例如 `andi $t2, 15, 7` 并不存在），只能先把立即数放进寄存器再计算。不确定指令是否存在时，一定先查指令集。

**取反小例**：先 `li $t0, 0x0000000f`，再执行 `nor $t0, $t0, $0`，`$t0` 就变成 `0xfffffff0`。

### 加减法：补码、溢出与异常

四则运算有两个棘手问题：**正负性**和**位溢出**。补码的优越之处在于：**只要允许舍去溢出的最高位，就不必先判断正负，直接按二进制加减即可**。

| 例子（32 位） | 直接计算 | 舍去溢出位后的结果 |
| --- | --- | --- |
| `0x3 + 0xffffffff` | `0x100000002` | `0x2` |
| `0xfffffffd - 0xffffffff` | 被减数前面补一个溢出位 1 → `0x1fffffffd - 0xffffffff` | `0xfffffffe` |

所以四条加减法指令的区别在于**是否检查溢出**：

| 指令 | 溢出时的行为 |
| --- | --- |
| `addu`、`subu` | **不检查溢出**：出现溢出直接舍去最高位，简单粗暴 |
| `add`、`sub` | 心思缜密：发生溢出**不计算，直接报错、抛出异常** |

> **为什么 `add`/`sub` 反而更容易「溢出」？** 按补码直接算时，`3 + (-1)`、`(-3) - (-1)` 本来都是十以内的加减法，但计算过程中出现了「补溢出位」的操作，于是产生了**本来没溢出却溢出**的假象。因此判断是否真的溢出时，还是要规规矩矩按「同号相加 / 异号相减」的老方法来做。

```asm
addu $t2, $t0, $t1    # 只保留低 32 位，不报错
subu $t2, $t0, $t1    # 同上
add  $t2, $t0, $t1    # 溢出则抛异常
addi $t2, $t0, imm16  # 立即数版本，addiu 同理
```

- **`subi`、`subiu` 是伪指令**，标准指令集中不存在，由汇编器展开。
- `addi`/`addiu` 的立即数为 **16 位**；写出**超过 16 位的立即数是不合法的**（会变成伪指令、被展开成多条指令），期末理论考试中要能判断出来。

**溢出小例**：当 `$t0 = 0x7fffffff`（最大的正数）时

```asm
addi  $t1, $t0, 1    # 结果会是 0x80000000（变成负数）→ 有符号溢出，抛异常
addiu $t2, $t0, 1    # 不检查溢出，$t2 = 0x80000000
```

### 乘除法：HI 与 LO 的分工

两个 32 位数相乘最多能得到 **64 位**结果，装不进一个寄存器，于是把结果拆成高 32 位和低 32 位，分别存入 `HI` 和 `LO`：

| 运算 | `HI` | `LO` |
| --- | --- | --- |
| `mult` / `multu` | 乘积的**高** 32 位 | 乘积的**低** 32 位 |
| `div` / `divu` | **余数** | **商** |

```asm
mult $t0, $t1     # 只需指定两个乘数，结果自动进 HI/LO
mfhi $t2          # $t2 ← HI（高 32 位）
mflo $t3          # $t3 ← LO（低 32 位）
mthi $t4          # 反向：把 $t4 写入 HI
mtlo $t5          # 反向：把 $t5 写入 LO
div  $t0, $t1     # $t0 ÷ $t1：LO 存商、HI 存余数（很容易记反）
```

> `HI`/`LO` 是特殊寄存器，**不能直接参与运算**，必须先用 `mfhi`/`mflo` 把数据取到通用寄存器中。

**`mult` 是有符号乘法**，例如 `0xffffffff * 0xffffffff`（即 `(-1) × (-1)`）的结果是：

```text
HI = 0x00000000    LO = 0x00000001
```

而**不是** `HI = 0xfffffffe, LO = 0x00000001`。

**为什么加减法可以不在乎有无符号、乘法却不行？** 其实二者是一致的：只看**低 32 位（`LO`）**时，两种算法结果完全相同——上面这个例子的 `LO` 都是 `0x00000001`；而 `HI` 是「溢出」出来的高 32 位，两种算法结果不同是正常的（正如加法里被舍去的那个溢出位）。

**除法余数的正负**（与 C 语言规定相同）：**被除数为正数时余数为 0 或正数；被除数为负数时余数为 0 或负数。**

| 被除数 | 除数 | 商 | 余数 |
| --- | --- | --- | --- |
| 7 | 3 | 2 | 1 |
| -7 | 3 | -2 | -1 |
| 7 | -3 | -2 | 1 |
| -7 | -3 | 2 | -1 |

**小例**：`-7 ÷ 3` 的结果可以直接对应到上表

```asm
li   $t0, -7
li   $t1, 3
div  $t0, $t1    # HI = -1（余数）、LO = -2（商）
mflo $t2         # $t2 = 0xfffffffe（-2）
mfhi $t3         # $t3 = 0xffffffff（-1）
```

### 移位（R 型）

| 指令 | 全称 | 含义 |
| --- | --- | --- |
| `sll` | **S**hift **L**eft **L**ogical | 逻辑左移 |
| `srl` | **S**hift **R**ight **L**ogical | 逻辑右移（高位补 0） |
| `sra` | **S**hift **R**ight **A**rithmetic | 算术右移（高位补符号位） |
| `sllv` | **S**hift **L**eft **L**ogical **V**ariable | 左移，移位量在寄存器中 |
| `srlv` | **S**hift **R**ight **L**ogical **V**ariable | 逻辑右移，移位量在寄存器中 |
| `srav` | **S**hift **R**ight **A**rithmetic **V**ariable | 算术右移，移位量在寄存器中 |

### 移位指令细节

| 指令形式 | 说明 |
| --- | --- |
| `sll`/`srl`/`sra` | 移位量是 **5 位立即数**（`imm5`），只能取 **0 ~ 31**，超出这个范围无法执行 |
| `sllv`/`srlv`/`srav` | 移位量放在寄存器中，把 `imm5` 换成寄存器即可 |

```asm
sll  $t1, $t0, 2     # $t1 = $t0 << 2
srl  $t1, $t0, 3     # 逻辑右移，高位补 0
sra  $t1, $t0, 3     # 算术右移，高位补符号位
sllv $t1, $t0, $s0   # 左移 $s0 寄存器中的值这么多位
```

> **移位量寄存器大于 31 时怎么办？** 不报错、也不按 31 封顶，而是**直接舍去该值的第 5 ~ 31 位，只保留最低 5 位**作为实际移位量。例如 `$s0 = 0xf0` 时，实际只移动 `0x10` 即 **16** 位。

**用移位代替乘除 2 的幂更快捷**：把 `$t0` 自乘 4——

```asm
# 用乘法：需要三步
li   $t1, 4
mult $t0, $t1
mflo $t0

# 用移位：只要一步
sll  $t0, $t0, 2
```

### 立即数运算（I 型）

| 指令 | 全称 | 含义 |
| --- | --- | --- |
| `addi` | **ADD** **I**mmediate | 加立即数（符号扩展） |
| `addiu` | **ADD** **I**mmediate **U**nsigned | 加立即数（不检查溢出） |
| `andi` | **AND** **I**mmediate | 与立即数（零扩展） |
| `ori` | **OR** **I**mmediate | 或立即数（零扩展） |
| `xori` | e**X**clusive **OR** **I**mmediate | 异或立即数（零扩展） |
| `slti` | **S**et on **L**ess **T**han **I**mmediate | 小于立即数则置 1 |
| `sltiu` | **S**et on **L**ess **T**han **I**mmediate **U**nsigned | 无符号比较版（立即数仍符号扩展） |
| `lui` | **L**oad **U**pper **I**mmediate | 把立即数装到高 16 位 |

**小例**：两类常见用法

```asm
andi $t0, $t1, 0xff    # 掩码：只保留 $t1 的最低 8 位，其余位清零
ori  $t0, $t1, 0x0f00  # 置位：把低 16 位中的第 8~11 位置 1，其余位不变
addi $t0, $t1, -1      # 立即数为负：符号扩展成 0xffffffff 后相加，相当于减 1
```

### `lui` 与 32 位立即数

每条指令的机器码固定为 32 位，如果一个 32 位立即数就把整个指令的空间占满了，其它信息（`op`、`rs`、`rt`……）就无处安放。所以**立即数一律限制在 16 位以内**，超过 16 位的常数只能分两部分写入：先 `lui` 写高 16 位，再 `ori` 写低 16 位。

```asm
lui $t0, 0x1234       # 高 16 位：$t0 ← 0x12340000
ori $t0, $t0, 0x5678  # 低 16 位：$t0 ← 0x12345678
```

`lui` 的功能就是**把立即数左移 16 位后写入寄存器**（低 16 位补 0）：`lui $t0, 0x1234` 的结果是 `$t0 = 0x12340000`。

## 系统调用与伪指令

除了运算、访存、跳转三类指令，还需要系统调用、协处理器指令与伪指令才能支撑一个完整的程序。

> **术语提示**：中文资料里的「伪指令」被用在两个完全不同的地方，读到时请注意区分——
> - **扩展指令（Pseudo Instructions）**：汇编器封装或转写出来的「假指令」，如 `li`、`la`、`move`；汇编时会被展开成标准指令。本文多处按习惯也把它们叫「伪指令」。
> - **汇编指示（Directives）**：如 `.data`、`.text`、`.word`、`.asciiz`、`.space`；它们**不是指令，不产生机器码**，只用来指导汇编器组织程序。部分教材也把它译作「伪指令」。

### 系统与协处理器

| 指令 | 全称 | 含义 |
| --- | --- | --- |
| `syscall` | **SYStem CALL** | 系统调用（调用号放在 `$v0`） |
| `break` | **BREAK** | 断点（调试用） |
| `nop` | **No** **Op**eration | 空操作 |
| `eret` | **E**xception **RET**urn | 异常返回 |
| `mfc0`、`mtc0` | **M**ove **F**rom / **T**o **C**oprocessor **0** | 读 / 写 CP0 寄存器 |
| `mfc1`、`mtc1` | **M**ove **F**rom / **T**o **C**oprocessor **1** | 通用寄存器与浮点寄存器间搬数 |
| `lwc1`、`swc1` | **L**oad / **S**tore **W**ord **C**oprocessor **1** | 浮点数访存 |
| `cvt.s.w` | **C**on**V**er**T** **S**ingle from **W**ord | 整型转单精度浮点 |

**小例**：用 `syscall` 打印一个整数再退出

```asm
li  $v0, 1        # 1 号系统调用：打印整数
li  $a0, 42       # 要打印的值放在 $a0
syscall           # 在 Run I/O 区输出 42
li  $v0, 10       # 10 号系统调用：退出程序
syscall
```

### 常见伪指令

| 指令 | 全称 | 展开为 |
| --- | --- | --- |
| `li` | **L**oad **I**mmediate | `lui` + `ori`（或一条 `addiu`/`ori`） |
| `la` | **L**oad **A**ddress | `lui` + `ori` |
| `move` | **MOVE** | `addu rd, rs, $zero` |
| `not` | **NOT** | `nor rd, rs, $zero` |
| `neg` | **NEG**ate | `sub rd, $zero, rs` |
| `abs` | **ABS**olute value | 取绝对值（分支 + 减法） |
| `mul` | **MUL**tiply | `mult` + `mflo` |
| `rem` | **REM**ainder | `div` + `mfhi` |
| `bgt`、`blt` | **B**ranch if **G**reater **T**han / **L**ess **T**han | `slt $at, …` + `bne` |
| `bge`、`ble` | **B**ranch if **G**reater / **L**ess than or **E**qual | `slt $at, …` + `beq` |
| `beqz`、`bnez` | **B**ranch if **EQ**ual / **N**ot **E**qual to **Z**ero | `beq`/`bne` + `$zero` |
| `seq`、`sne` | **S**et if **EQ**ual / **N**ot **E**qual | `slt`/`sltu` 等 + `xori 1` |
| `sge`、`sgt`、`sle` | **S**et if **G**reater or **E**qual / **G**reater **T**han / **L**ess or **E**qual | `slt`/`sltu` 等组合 |
| `sgeu`、`sgtu`、`sleu` | 上述三条的**无符号**版本 | 用 `sltu` 等组合 |

> 记忆口诀：**i 是立即数，u 是无符号，v 是移位量在寄存器，z 是与零比较；l/s 前缀是读/写内存，b/h/w 后缀是字节/半字/字。**

### 伪指令（扩展指令）

伪指令（pseudo-instruction，MARS 中也称扩展指令）**不是真实指令**，CPU 不认识它们；汇编器（MARS）会在汇编阶段把它们展开成一条或多条 R/I/J 型真实指令。

**标准指令与扩展指令**：一条机器码可以还原成一条「完美对应」的汇编指令，例如 `0x08000c0a` ↔ `j 0xc0a`——每个操作数都与机器码中的字段一一对应，这种形式称为**标准指令（基本指令）**。全部使用标准指令会大大降低程序的易读性与灵活性，于是诞生了扩展指令。扩展指令主要有两类来源：

| 来源 | 做法 | 例子 |
| --- | --- | --- |
| **封装**：提供标准指令集没有的常用功能 | 用若干条基本指令组合出新指令 | `mul`、`rem`、`not`、`abs` |
| **转写 / 略写**：改变已有指令的操作数形式或个数 | 换一种更简洁的写法 | `j Label`（用标签代替 26 位立即数）、`beqz`、`move`、`li` |

**`li` 的两种展开**：汇编器会根据立即数的大小选择展开方式——立即数不超过 16 位时只展开为**一条 `addiu`**（或 `ori`，视数值而定）；超过 16 位时展开为 `lui` + `ori`。

```asm
li $a0, 100          # ≤ 16 位 → addiu $a0, $0, 100
li $a0, 0x12345678   # > 16 位 → lui + ori 两条指令
```

**`la` 不能用 `li` 代替**：`li` 只能装立即数，要装标签地址必须用 `la`。`la $t0, fibs` 就是把标签 `fibs` 代表的 **32 位地址**存入 `$t0`。

```asm
la $t0, fibs      # $t0 ← fibs 的地址（不是数组里的第一个元素）
lw $t1, 0($t0)    # $t1 ← fibs[0]
```

**转写小例**：把 `j 0xc0a` 写成 `j loop` 后，汇编器会在汇编时算出标签对应的立即数，再还原成基本指令的形式；`li`、`la`、`move` 等也都是「先写简单的，汇编时再还原」。

> 想查更多扩展指令：MARS 的 Help 文档中 **Extended (pseudo) Instructions** 一栏。

常见伪指令及其展开如下：

| 伪指令 | 展开为 | 说明 |
| --- | --- | --- |
| `li rd, imm` | `lui` + `ori`（能装入 16 位时只用一条 `addiu`/`ori`） | 装入 32 位常数 |
| `la rd, label` | `lui` + `ori` | 装入 32 位地址 |
| `lw rd, label`、`sw rs, label` | `lui $at, …` + `lw`/`sw` | 直接用标签地址访存 |
| `move rd, rs` | `addu rd, rs, $zero` | 寄存器间传送 |
| `not rd, rs` | `nor rd, rs, $zero` | 按位取反 |
| `neg rd, rs` | `sub rd, $zero, rs` | 取负（无符号版为 `negu`） |
| `subi rd, rs, imm` | `addi rd, rs, -imm` | 减去立即数（标准指令集中不存在） |
| `mul rd, rs, rt` | `mult rs, rt` + `mflo rd` | 三操作数乘法 |
| `div rd, rs, rt`、`rem rd, rs, rt` | `div rs, rt` + `mflo rd` / `mfhi rd` | 三操作数除法、取余 |
| `bgt`、`blt`、`bge`、`ble` | `slt $at, …` + `bne`/`beq` | 无对应真实分支指令，靠 `slt` 加条件分支实现 |
| `beqz`、`bnez` | `beq`/`bne` + `$zero` | 与 0 比较的分支 |
| `rol`、`ror rd, rs, imm` | `srl`/`sll` + `sll`/`srl` + `or` | 循环移位 |
| `ulw`、`usw` | `lwl` + `lwr` / `swl` + `swr` | 未对齐访存 |
| `nop` | `sll $0, $0, 0` | 空操作 |

此外还有 `abs`、`seq`、`sne`、`sge`、`sgt`、`sle` 等组合类伪指令，展开为若干条真实指令的组合。

> **注意**：不少伪指令在展开时会用到 `$at`（`$1`）甚至 `$zero`，这正是前面「寄存器使用约定」中说 `$at` 要留给汇编器、不要自己使用的原因。

### 伪指令展开实例（在 MARS 中观察）

把下面的代码存进 MARS，点上方「扳手和螺丝刀」图标进入 **Execute** 界面，就能在左上方看到每条伪指令被「翻译」成了什么：

```asm
ori $t1, $0, 0xf
ori $t1, $0, 0xfffff
ori $t1, 0xf
li   $t2, 5
move $t3, $t2
label1:
la   $t4, label1
```

| 写法 | 展开结果 |
| --- | --- |
| `ori $t1, $0, 0xf` | 不变，本身就是合法指令 |
| `ori $t1, $0, 0xfffff` | **展开为 3 条指令**：`0xfffff` 有 20 位，**超出 `ori` 的 16 位立即数上限**，因此不合法（MARS 帮你处理了，但理论考试要能判断） |
| `ori $t1, 0xf` | 少写了一个寄存器，MARS 把它解读为 `$t1 = $t1 \| 0xf`（即 `$t1 |= 0xf`） |
| `li $t2, 5` | 伪指令：把**立即数**装入寄存器 |
| `move $t3, $t2` | 伪指令：展开为 `addu $t3, $t2, $zero` |
| `la $t4, label1` | 伪指令：把**标签的地址**装入寄存器（`label1` 被替换为它的地址 `0x00003008`） |

> **`move` 是 copy，不是物理搬移**：执行后原寄存器的值**不会被清零**。
>
> **`li` 与 `la` 的分工**：`li` 装入立即数，`la` 装入地址。
>
> **标签本身不是汇编指令**，只是一种便捷写法，汇编器会把它替换成地址。

## 汇编指示（Directives）

**汇编指示（Directives）** 用来指导汇编器如何处理程序，作用类似其他语言中的预处理命令。它**不是指令**：既不会被翻译成机器码，也不会被 CPU 执行，但会影响其它指令的汇编结果（例如把数据放进哪个段、把 `la`/`lw` 中的标签换成什么地址）。

有了它，才能初始化变量（数据段中的数据）、声明全局标签、定义宏、指定异常处理的代码段与数据段。常用指示如下：

| 指示 | 作用 | 小例 |
| --- | --- | --- |
| `.data` | **数据段**的开始标志，用于预先存储数据 | `.data` |
| `.text` | **代码段**的开始标志，程序指令从这里开始 | `.text` |
| `.word` | 以**字**（4 字节）为单位存储数据 | `num: .word 42` |
| `.asciiz` | 以**字节**为单位存储字符串，末尾自动补 `\0` | `msg: .asciiz "Hello"` |
| `.space` | 申请若干个字节的**未初始化**内存空间 | `buf: .space 16` |

此外还有声明全局标签（`.globl main`）、定义宏（`.macro`）、指定异常处理代码段/数据段（`.ktext` / `.kdata`）等。更详细的用法见后续「变量的声明与定义」一节，更多指示可查 MARS 的 Help 文档中 **Directives** 一栏。

**小例**：一个带数据段的完整程序——先打印字符串，再打印一个整数，最后退出

```asm
.data
msg:  .asciiz "Hello, MIPS!\n"   # 以字节为单位存储字符串，末尾自动补 \0
num:  .word 42                   # 以字为单位存储一个整数
buf:  .space 16                  # 预留 16 字节未初始化的空间

.text
main:
    li   $v0, 4                  # 4 号系统调用：打印字符串
    la   $a0, msg
    syscall

    lw   $a0, num                # 从数据段取回 42
    li   $v0, 1                  # 1 号系统调用：打印整数
    syscall

    li   $v0, 10                 # 10 号系统调用：退出程序
    syscall
```

> **对照**：`msg`、`num`、`buf` 都是**数据标签**，依次对应数据段中的字符串、一个字、16 字节空间；`la $a0, msg` 与 `lw $a0, num` 都是扩展指令，会在汇编阶段被展开（见前文「伪指令（扩展指令）」）。

## 指令的书写格式

指令一般以**指令名**开头，后跟该指令的**操作数**，中间用空格或逗号隔开。操作数一般为 0–3 个，且每条指令的操作数个数是固定的：

- 一般形式：`指令名 操作数1, 操作数2, 操作数3`
- 存取类指令：`指令名 操作数1, 操作数3(操作数2)`

操作数即指令操作所作用的实体，可以是寄存器、立即数或标签：

- **立即数**：指令中设定好的常数，可直接参与运算，一般长度为 16 位。
- **标签**：表示一个地址，供指令引用，一般用于数据存取的地址（类似数组名）或程序跳转的地址（类似函数名）。写法为 `name:`，名称可自行取定；汇编时标签会由汇编器转换成立即数。

常见的指令写法：

```asm
add $s0, $a0, $a1
addi $s0, $a0, 12
mult $s1, $s2
beq $a1, $a2, -2
blez $s1, -2
jr $ra
j 0x00003014
```

由于标签可以代替某个地址，也可以写成：

```asm
beq $a1, $a2, loop
j loop
```

这里的 `loop` 是一个标签，代表一段代码的起始地址；汇编器会自动把标签转换成所需的立即数，不用自己计算地址偏移量，简化了编程。

> **注意**：在 MARS 中，跳转指令只能使用标签跳转，不能使用立即数！

可见操作数的形式并非绝对固定，而是有一定灵活度：MIPS 标准指令集中一条机器码指令的格式是固定的，但汇编器可以把多种形式的汇编指令转换为含义相同的机器码，因此许多指令有比标准写法更简单的写法（详见「扩展指令」，或查阅 MARS 帮助文档）。

### 操作数的个数

指令的操作数个数并不固定，可以是 0 个、1 个、2 个或 3 个：

| 指令 | 操作数个数 | 说明 |
| --- | --- | --- |
| `nop` | 0 | 空操作 |
| `syscall` | 0 | 系统调用 |
| `j label` | 1 | 无条件跳转 |
| `jr $ra` | 1 | 寄存器跳转 |
| `add $t0, $t1, $t2` | 3 | 加法 |

所以并不是每条指令都至少有两个操作数，很多指令并不需要使用 32 个通用寄存器中的任何一个：

- `j label`：只修改程序计数器 PC，不使用通用寄存器。
- `syscall`：指令本身没有寄存器操作数；虽然按约定系统调用号放在 `$v0`，但这是软件约定，不是指令格式的强制要求。
- `nop`：通常编码为 `sll $0, $0, 0`，严格看编码会涉及 `$zero`，但从助记符和语义上看可视为不使用通用寄存器。
- 更稳妥的反例是 `j label`，它只依赖 PC，不依赖 32 个基础通用寄存器。

> 如果把 PC 也算作「寄存器」，那所有指令都会用到 PC；但通常说的「32 个基础寄存器」指的是通用寄存器，不包括 PC。

MIPS 机器码指令的格式（R/I/J 型）见后文「指令格式」。

## 标签

标签（Label）是一个**符号名**，用来代表某个**内存地址**或**指令地址**。它本身不占用内存空间，也不会被翻译成机器指令，只是汇编器为了方便程序员书写和阅读而提供的「名字」。

### 标签的写法

标签通常写在指令或数据的前面，后面跟一个冒号 `:`：

```asm
label: instruction
```

也可以单独一行：

```asm
label:
    instruction
```

在数据段中：

```asm
.data
msg: .asciiz "Hello, MIPS!"
```

- `msg` 是**数据标签**，代表字符串 `"Hello, MIPS!"` 的首地址。
- `main:`、`loop:`、`exit:` 等是**代码标签**，代表某条指令的地址。

### 标签的作用

| 用途 | 示例 |
| --- | --- |
| 跳转目标 | `j loop`、`jal function` |
| 分支目标 | `beq $t0, $t1, equal`、`bne $t0, $t1, loop` |
| 数据引用 | `la $a0, msg`、`lw $t0, value` |
| 函数入口 | `main:`、`factorial:` |
| 程序结束点 | `exit:` |

简单说，标签让程序员不必手动计算和记忆具体的内存地址，直接用有意义的名字来引用。

### 代码标签与数据标签

**代码标签（指令标签）** 放在 `.text` 段中，代表某条指令的地址：

```asm
.text
main:
    li $v0, 4
    la $a0, msg
    syscall
    j exit

loop:
    addi $t0, $t0, 1
    bne $t0, $t1, loop

exit:
    li $v0, 10
    syscall
```

- `main`：程序入口；`loop`：循环开始地址；`exit`：程序退出代码地址。

**数据标签** 放在 `.data` 段中，代表数据的首地址：

```asm
.data
value:  .word 0x12345678
msg:    .asciiz "Hello"
array:  .space 40
```

- `value`：一个字（4 字节）数据的地址；`msg`：字符串首地址；`array`：40 字节空间的起始地址。

### 汇编器如何处理标签

汇编器在汇编过程中会维护一张**符号表（Symbol Table）**，记录每个标签的名字和它对应的地址。遇到引用标签的指令时，再按指令类型把标签转换成不同形式：

| 指令类型 | 示例 | 标签转换结果 |
| --- | --- | --- |
| 跳转指令 | `j Label`、`jal Label` | 26 位目标地址字段，实际存 `(Label 地址 >> 2) & 0x03FFFFFF` |
| 分支指令 | `beq $t0, $t1, Label` | 16 位有符号相对偏移，相对下一条指令 `PC+4`，以字为单位，即 `(Label - (PC+4)) / 4` |
| 加载地址 | `la $t0, Label` | 通常展开为 `lui` + `ori`，得到 32 位绝对地址 |
| 数据访问 | `lw $t0, Label` | 16 位立即数偏移，相对基址寄存器（通常是 `$gp` 或 `$sp`） |

因此，标签最终会变成**地址**或**地址偏移量**，编码进机器指令中。

**小例**：若 `loop` 的地址为 `0x00400010`

- `j loop` 的 26 位字段 = `0x00400010 >> 2` = `0x00100004`；
- 若 `beq $t0, $t1, loop` 位于 `0x00400000`，则分支偏移 = `(0x00400010 − 0x00400004) ÷ 4` = `3`（以**字**为单位的相对条数）。

### 命名规则与作用域

- 标签名由字母、数字、下划线组成，通常以字母或下划线开头。
- 标签名不能与 MIPS 指令助记符或寄存器名冲突。
- 标签名一般区分大小写（取决于汇编器）。
- 同一个标签不能重复定义。
- **局部标签**：某些汇编器支持数字标签（如 `1:`），可以用 `1b`（向前）和 `1f`（向后）引用，常用于编译器生成的代码，避免重名。

**小例**：

```asm
1:  addi $t0, $t0, 1
    bne  $t0, $t1, 1b   # 1b：向前找到最近的 1: 标签，不满足则继续循环
    blez $t0, 1f        # 1f：向后找到最近的 1: 标签
1:  nop
```

### 完整示例

```asm
.data
msg:    .asciiz "The sum is: "
newline: .asciiz "\n"

.text
.globl main

main:
    li $t0, 0          # sum = 0
    li $t1, 1          # i = 1
    li $t2, 10         # n = 10

loop:
    add $t0, $t0, $t1  # sum += i
    addi $t1, $t1, 1   # i++
    ble $t1, $t2, loop # if i <= n goto loop

    # 打印结果
    li $v0, 4
    la $a0, msg
    syscall

    move $a0, $t0
    li $v0, 1
    syscall

    li $v0, 4
    la $a0, newline
    syscall

exit:
    li $v0, 10
    syscall
```

其中：`msg`、`newline` 是数据标签；`main`、`loop`、`exit` 是代码标签；`la` 伪指令用数据标签加载地址；`ble` 分支用代码标签作为目标。

### 注意事项

- **标签不是变量**：它只是一个地址符号，不存储数据。
- **标签不占空间**：不会在内存中分配字节。
- **分支范围有限**：16 位偏移意味着分支只能跳到附近约 ±128 KB 范围内，超出范围需要用 `j` 跳转。
- **跳转范围有限**：26 位地址字段，目标地址必须与当前 `PC+4` 的高 4 位相同。
- **链接器重定位**：如果程序由多个模块组成，链接器会最终确定标签的绝对地址并进行重定位。

> **一句话总结**：MIPS 中的标签是汇编器使用的符号地址，用来标识指令或数据的位置。它让程序员可以用名字跳转和访问数据，汇编器会将其转换为机器指令中的绝对地址或相对偏移量。

## 立即数

**立即数（Immediate）** 是指**直接编码在指令中的常数**，是操作数的一种。它不需要从寄存器或内存中读取，而是随指令一起被 CPU 取出并使用。简单说，立即数就是「写死在指令里的数字」。

### 基本例子

```asm
addi $t0, $t1, 100
```

这条指令中，`$t0`、`$t1` 是**寄存器操作数**，`100` 就是**立即数**，含义为 `$t0 = $t1 + 100`；`100` 直接出现在指令编码中，不需要额外去内存取。

```asm
ori $t0, $t1, 0xFF
lui $t0, 0x1234
```

其中 `0xFF`、`0x1234` 也都是立即数。

### 立即数的特点

| 特点 | 说明 |
| --- | --- |
| 直接编码在指令中 | 不需要访问寄存器或内存 |
| 速度快 | 随指令一起取出，适合做常量和简单运算 |
| 位数有限 | MIPS 中通常只有 16 位 |
| 需要扩展 | 参与运算时可能进行符号扩展或零扩展 |
| 不是所有常数都能直接表示 | 32 位常数通常需要 `lui` + `ori` 组合 |

在 MIPS 中，算术/逻辑立即数指令（如 `addi`、`andi`、`ori`）的立即数字段一般是 **16 位**：

- `addi` 使用**符号扩展**，范围约为 `-32768 ~ 32767`；
- `andi`、`ori` 使用**零扩展**，范围约为 `0 ~ 65535`。

**超范围小例**：`addi $t0, $t0, 0x12345` 中的 `0x12345` 有 17 位，**超出 16 位字段，不能直接编码**；应改成先把常数装入寄存器：

```asm
li  $t1, 0x12345
add $t0, $t0, $t1
```

### 符号扩展与零扩展

16 位立即数参与运算前必须先扩展成 32 位：

- **符号扩展**：把最高位（符号位）复制到高 16 位，保持**有符号数**的值不变；
- **零扩展**：高 16 位直接填 0，保持**无符号数/位串**的值不变。

以 16 位立即数 `0xFF9C` 为例：符号扩展得 `0xFFFFFF9C`（−100），零扩展得 `0x0000FF9C`（65436）。同一串 16 位数据，扩展方式不同，得到的数值就不同；用哪种由**指令本身**决定。

**记忆规律**：

| 类别 | 扩展方式 | 指令 |
| --- | --- | --- |
| 算术立即数 | **符号扩展** | `addi`、`addiu`、`slti`、`sltiu` |
| 访存偏移 | **符号扩展** | `lw`、`sw`、`lh`、`sh`、`lb`、`sb` |
| 分支偏移 | **符号扩展** | `beq`、`bne`、`blez`、`bgtz` |
| 逻辑立即数 | **零扩展** | `andi`、`ori`、`xori` |
| 高 16 位装载 | 立即数左移 16 位，低 16 位补 0 | `lui` |

一句话：**只有逻辑类立即数（`andi`／`ori`／`xori`）零扩展，其余一律符号扩展。**

之所以这样分工：算术指令要能表示负数（`addi $t0, $t0, -1` 靠立即数 `0xFFFF` 扩展成 `0xFFFFFFFF`）；访存偏移与分支偏移也要能为负（`lw $t0, -4($sp)`、向前跳转）；而逻辑指令的立即数是位掩码，不能被符号污染（`ori $t0, $t0, 0x8000` 期望只置第 15 位，得到 `0x00008000`）。

**同一个 `0xffff`，两种结果**：

```asm
addi $t0, $t1, 0xffff   # 符号扩展 → 0xffffffff（即 -1），相当于「减 1」
andi $t0, $t1, 0xffff   # 零扩展   → 0x0000ffff，相当于「只保留最低 16 位」
```

### 补充例题：哪些指令会对立即数做符号扩展？

> 下列指令操作过程中，需要将立即数符号扩展的是（多选）：
>
> A. `beq $s2, $s3, 4`　B. `lw $s2, 4($s3)`　C. `ori $s2, $s3, 4`　D. `addi $s2, $s3, 4`　E. `sltiu $s1, $s2, 0x8888`

**答案：A、B、D、E**（只有 C 是零扩展）。

| 选项 | 立即数的去处 | 扩展方式 |
| --- | --- | --- |
| A `beq` | 16 位分支偏移，符号扩展后左移 2 位，加到 `PC+4` | 符号扩展 |
| B `lw` | 16 位偏移，符号扩展后与基址寄存器相加，形成有效地址 | 符号扩展 |
| C `ori` | 16 位立即数零扩展后与 `rs` 按位或 | 零扩展 |
| D `addi` | 16 位立即数符号扩展后与 `rs` 相加 | 符号扩展 |
| E `sltiu` | 16 位立即数符号扩展后，再按**无符号**方式比较 | 符号扩展 |

**易错点（E）**：`sltiu` 名字里的 `u` 指的是**比较时按无符号数进行**，而不是「立即数零扩展」。手册伪码为

```text
GPR[rt] = (GPR[rs] < sign_extend(immediate)) ? 1 : 0
```

以本题的 `0x8888` 为例（最高位是 1）：

| 处理方式 | 立即数变成 | 作为无符号数 |
| --- | --- | --- |
| 符号扩展（手册规定） | `0xFFFF8888` | 4294934664 |
| 零扩展（误以为） | `0x00008888` | 34952 |

所以该指令实际比较的是 `$s2 < 4294934664`（几乎恒为真）；若误按零扩展理解成 `$s2 < 34952`，结论会完全不同。

### 操作数类型的对比

| 操作数类型 | 例子 | 含义 |
| --- | --- | --- |
| 寄存器 | `$t0`、`$a0` | 数据在 CPU 寄存器中 |
| 立即数 | `100`、`0xFF` | 数据直接写在指令中 |
| 内存 | `0x10010000` | 数据在内存中，需要加载 |

**小例**：一条指令里可以同时出现多种操作数

```asm
addi $t0, $t1, 100   # $t1 是寄存器操作数，100 是立即数操作数
lw   $t0, 4($sp)     # $sp 是基址寄存器，4 是偏移立即数，真正的数据在内存中
```

### 标签与立即数的关系

标签作为操作数时，最终也会转化为**立即数形式**：

- **分支指令** `beq $t0, $t1, Label`：标签转化为 16 位相对偏移量，编码在立即数字段中。
- **跳转指令** `j Label`：标签转化为 26 位目标地址字段，也属于指令中直接给出的常数形式。
- **加载地址** `la $t0, Label`：通常展开为 `lui` + `ori`（见下）。

所以标签最终变成的是**立即数**，而不是寄存器，也不是指令本身。

### 伪指令 `li` 与立即数

```asm
li $t0, 0x12345678
```

`li` 是伪指令，不是真实指令。因为 32 位常数无法塞进一条 MIPS 指令的 16 位立即数字段，所以汇编器会把它拆成两条指令：

```asm
lui $t0, 0x1234
ori $t0, $t0, 0x5678
```

以此组合出 32 位常数。

> **一句话总结**：立即数就是直接编码在指令中的常数，速度快但位数有限；MIPS 中标签最终转化成的地址或偏移量，也是以立即数字段的形式出现在机器指令中。

## 认识机器码

> 计算机只能理解二进制形式的数据。前面所说的汇编语言，最终会转化为机器语言——也就是**机器码指令**，CPU 可以直接识别这种机器语言，从而去完成相应的操作。

在 MIPS 汇编中，**所有指令长度均为 32 位**，即 4 字节（1 字）。从硬件的角度讲，每条指令的执行周期大多为 1 个 CPU 周期（深入学习之后可以更好地理解，这里只是阐述一个概念）。因此机器码是 CPU 最基本的一种操作，也是**原子操作，不可被打断**。

所有指令长度均相同，这是**精简指令集（RISC，Reduced Instruction Set Computing）**的特征，采用这种指令集的有 MIPS 和手机中常用的 ARM 等；与之相对的是**复杂指令集（CISC，Complex Instruction Set Computing）**，包括 PC 中常用的 x86 架构，其特点是**指令数目多、指令长度并不完全相同**。

| 对比项 | RISC（MIPS、ARM） | CISC（x86） |
| --- | --- | --- |
| 指令长度 | 定长：MIPS 全为 32 位 | 变长：并不完全相同 |
| 指令数目 | 少而精简 | 多而复杂 |
| 译码难度 | 简单，适合流水线 | 复杂 |

一段汇编语言可以转换为一段机器码。例如下面这段汇编指令：

```asm
.data
.text
.global main
main:
addi $t0, $0, 100
ori $t1, $0, 200
add $t2, $t1, $t2
sub $t3, $t2, $t1
lui $t4, 233
ori $v0, 1
ori $a0, 2333
mthi $t1
syscall
nop
loop:
j loop
nop
```

其转换后的结果为（16 进制）：

```text
20080064
340900c8
012a5020
01495822
3c0c00e9
34420001
3484091d
01200011
0000000c
00000000
08000c0a
00000000
```

逐条对应关系如下（`.data`、`.text`、`.global` 是汇编指示，`main:`、`loop:` 是标签，它们都不产生机器码）：

| 汇编指令 | 机器码（16 进制） | 指令格式 |
| --- | --- | --- |
| `addi $t0, $0, 100` | `20080064` | I 型 |
| `ori $t1, $0, 200` | `340900c8` | I 型 |
| `add $t2, $t1, $t2` | `012a5020` | R 型 |
| `sub $t3, $t2, $t1` | `01495822` | R 型 |
| `lui $t4, 233` | `3c0c00e9` | I 型 |
| `ori $v0, 1` | `34420001` | I 型 |
| `ori $a0, 2333` | `3484091d` | I 型 |
| `mthi $t1` | `01200011` | R 型 |
| `syscall` | `0000000c` | R 型 |
| `nop` | `00000000` | R 型（编码为 `sll $0,$0,0`） |
| `j loop` | `08000c0a` | J 型 |
| `nop` | `00000000` | R 型 |

### 指令在内存中的位置（`.data` 与 `.text`）

不仅 `sw`/`sh`/`sb` 写入内存的数据会保存在内存中，**我们编写的每一条指令也都会被转换为二进制码保存在内存中**：

| 段 | 地址范围 | 存放内容 |
| --- | --- | --- |
| `.data` 段 | `0x00000000` ~ `0x00002fff` | 程序中写入或声明（`.word` 等）的数据 |
| `.text` 段 | `0x00003000` 之后 | 每条指令的**机器码**（即 MARS 代码区每行的 Code 列） |

- 在 Execute 界面把内存地址调到 `0x00003000` 开始的 `.text` 段，就能看到这一部分内存已被写入，且内容与代码区每行的 **Code** 完全对应。
- 因为每条指令固定 32 位，所以指令的 Address 也会按顺序**每条 +4** 递增（`0x3000`、`0x3004`、`0x3008`…）。
- 由此可知，跳转标签被翻译成的 `0x00003008` 之类的值，正是**目标指令的 Address**。

**把机器码导出成 txt**：MARS 左上角 `File` → `Dump Memory`，选择要导出的段（通常是 `.text`）与格式（一般选十六进制），再填文件名与保存路径，就能把所有指令的机器码导出成文本，方便在自己的 CPU 上运行和比对。

## 指令格式

> MIPS指令格式主要有三种：R型、I型和J型。每种格式都有不同的字段，用于表示操作码、寄存器、立即数等信息。

32 位的机器码需要一定的格式才能被理解。一般来说，在 MIPS 指令集中，指令分为三种格式：R 型、I 型和 J 型。三种格式都是 **32 位定长指令**，字段布局如下（左侧为高位，右侧为低位）：

| 格式 | 字段布局（共 32 位） | 常见指令 | 用途 |
| --- | --- | --- | --- |
| R 型 | `op(6)` `rs(5)` `rt(5)` `rd(5)` `shamt(5)` `funct(6)` | 算术/逻辑：`add`、`addu`、`sub`、`subu`、`and`、`or`、`xor`、`nor`、`slt`、`sltu`<br/>移位：`sll`、`srl`、`sra`、`sllv`、`srlv`、`srav`<br/>乘除：`mult`、`multu`、`div`、`divu`、`mfhi`、`mflo`、`mthi`、`mtlo`<br/>寄存器跳转：`jr`、`jalr`<br/>特殊：`syscall`、`break` | 寄存器之间的运算，不涉及立即数（`op` 固定为 `000000`，由 `funct` 区分具体操作） |
| I 型 | `op(6)` `rs(5)` `rt(5)` `immediate(16)` | 立即数运算：`addi`、`addiu`、`andi`、`ori`、`xori`、`slti`、`sltiu`、`lui`<br/>访存：`lw`、`sw`、`lb`、`lbu`、`lh`、`lhu`、`sb`、`sh`（未对齐访问还有 `lwl`、`lwr`、`swl`、`swr`）<br/>比较跳转：`beq`、`bne`、`blez`、`bgtz`、`bltz`、`bgez` | 含 16 位立即数或地址偏移：与立即数运算、访存、条件分支 |
| J 型 | `op(6)` `address(26)` | `j`（`op = 000010`）、`jal`（`op = 000011`） | 无条件跳转，26 位地址与 `PC+4` 的高 4 位拼接成目标地址 |
| **伪指令** | 无独立格式，汇编器会展开为上述 R/I/J 型真实指令 | `li`、`la`、`lw`/`sw`（带标签）、`move`、`not`、`neg`、`subi`、`subiu`、`mul`、`rem`、`bgt`、`blt`、`bge`、`ble`、`beqz`、`rol`、`ror`、`ulw`、`usw`、`nop` | 方便书写；CPU 不认识，由汇编器（MARS）在汇编阶段展开成一条或多条真实指令 |

### R 型指令

R 型指令的操作数最多，一般用于**运算指令**，例如 `add`、`sub`、`sll` 等。

![R 型指令格式](https://hep-xisi.oss-cn-beijing.aliyuncs.com/thumb/beihang/computer-org/chapter02/Mips-2-2.2.3.3.svg)

- 没有立即数，运算只在寄存器之间进行；
- `op` 固定为 `000000`，与 `funct` 一起决定具体操作。

**参数位置分几类**（写机器码时按 `rs, rt, rd, shamt` 的顺序排）：

| 书写形式 | 指令 |
| --- | --- |
| `xxx rd, rs, rt` | `add`、`addu`、`sub`、`subu`、`and`、`or`、`xor`、`nor`、`slt`、`sltu` |
| `sll rd, rt, shamt` | `sll`、`sra`、`srl` |
| `sllv rd, rt, rs` | `sllv`、`srav`、`srlv` |
| `jalr rd, rs` | `jalr` |
| `mult rs, rt` | `mult`、`multu`、`div`、`divu` |
| `mthi rs` | `mthi`、`mtlo`、`jr` |
| `mfhi rd` | `mfhi`、`mflo` |
| `syscall` | `syscall`、`break` |

**赋值规律**：有 `rd` 时一定是 `rd` 被赋值；`rs` **永远不能**被赋值；`rt` 视指令而定（`add` 不赋值，`sll` 会被赋值）。这条规律在后续设计 CPU 时很有用。

> `shamt` 只出现在 `sll`/`sra`/`srl` 三条指令中（5 位，表示 0~31 的位移量），其余 R 型指令中这 5 位恒为 `00000`。
>
> **记法**：R 型指令不一定有 3 个寄存器，但有 3 个寄存器的指令一定是 R 型。

### I 型指令

I 型指令的特点是有一个 **16 位的立即数**（偏移也是同样的道理）。因此，I 型指令一般用于：

- **与立即数相运算的指令**：`addi`、`subi`、`ori` 等；
- **比较跳转指令**：`beq`、`bgtz` 等——它们要让两个寄存器的值相比较，并让 PC 偏移 `offset` 这么多，刚好利用了全部的字段；
- **存取指令**：`sw`、`lw`，它们在使用时需要对地址指定一个偏移值，也会用到立即数字段。

> **记忆**：**6 条访存指令 + 所有 b 家族和 i 家族指令**都是 I 型（`sub`、`mfhi` 这种只是名字里带 b/i 字母，不算）。

![I 型指令格式](https://hep-xisi.oss-cn-beijing.aliyuncs.com/thumb/beihang/computer-org/chapter02/Mips-2-2.2.3.4.svg)

> **注意**：在写汇编语言的时候，需要使用**负号**来标记负数，而不要和机器码一样认为首位的 1 就代表负数。

- 16 位立即数按指令不同做**符号扩展**（如 `addi`）或**零扩展**（如 `andi`、`ori`）；
- 在分支指令中，它是相对 `PC+4` 的**字偏移**（详见后文「立即数的处理方式」）。

**书写顺序 ≠ 字段顺序**（机器码中永远是 `op`、`rs`、`rt`、`imm16`）：

| 家族 | 书写形式 | 备注 |
| --- | --- | --- |
| b 家族 | `beq rs, rt, label` | 与机器码顺序一致 |
| i 家族 | `ori rt, rs, imm16` | 目标寄存器 `rt` 写在 `rs` 前面，与机器码相反 |
| 访存家族 | `sw rt, imm16(rs)` | 顺序完全打乱，最容易写错 |

> **补丁**：`bltz`、`blez`、`bgtz`、`bgez` 只有一个寄存器，形如 `blez rs, imm16`。其中 `blez`/`bgtz` 的 `rt` 为 `00000`；`bltz` 与 `bgez` 的 `op` 相同（都是 `000001`），靠 `rt` 区分（`bltz` 为 `00000`、`bgez` 为 `00001`）。这几条指令一般不需要自己实现，了解即可。

**赋值规律**：书写指令时，**要被赋值的寄存器一定写在最前面**（i 家族、访存家族都是如此）；而在 I 型指令中，若有寄存器被赋值，那必然是 `rt`——`rs` 不具备被赋值的功能。（原因到搭建 CPU 时自然就明白了；上机考试中也可能出现「反过来考」的题。）

**小例**：`ori $s0, $s1, 19`（已知 `op = 001101`）

```text
rs = $s1 = 17 = 10001    rt = $s0 = 16 = 10000    imm16 = 19 = 0x0013
= 001101 10001 10000 0000000000010011
= 0x36300013
```

### J 型指令

J 型指令很少，常见的为 `j` 和 `jal`。它们需要**直接跳转至某个地址**，而非利用当前的 PC 值加上偏移量计算出新的地址，因此需要的位数较多。

![J 型指令格式](https://hep-xisi.oss-cn-beijing.aliyuncs.com/thumb/beihang/computer-org/chapter02/Mips-2-2.2.3.5.svg)

- 26 位地址会与 `PC+4` 的高 4 位拼接成跳转目标，因此目标必须与当前 `PC+4` 处于同一 256 MB 区域内。

**小例**：地址 `0x00003004` 处的 `j target`，`target` 指向地址 `0x0000301c`：

```text
op = j = 000010
imm26 = 目标地址 >> 2 = 0x0000301c >> 2 = 0x0c07
机器码 = 000010 000000000000110000000111 = 0x08000c07
```

**26 位为什么够装下 32 位地址？** 靠两次「牺牲」：

1. 指令地址必然是 4 字节对齐，**最低 2 位恒为 0**，直接舍去 → 32 位变 30 位；
2. 30 位仍塞不进 26 位，只好牺牲跳转范围：**砍掉最高 4 位，用当前 PC 的最高 4 位代替** → 30 位变 26 位。

所以跳转目标必须与 PC 的高 4 位相同（即处于同一个 256 MB 区域）；若目标不在同一区域，只能改用 `jr`/`jalr`。

**补充：跳转范围为什么是 256 MB？**

`j` 的目标地址构造为：

```text
目标地址 = { PC+4 的高 4 位 , 26 位地址字段 , 00 }
                                        └─ 指令地址 4 字节对齐，最低 2 位恒为 0
```

- 26 位字段要**左移 2 位**（末尾拼两个 0）才是真正的字节地址，故实际参与寻址的是 **28 位**：`2²⁸` 字节 = **256 MB**。
- 但高 4 位取自 `PC+4` 且**不能改变**，所以 `j` 只能在**当前代码所在的 256 MB 区域**内跳转，并不是在整个 4 GB 空间内任意跳。

| 选项 | 大小 | 说明 |
| --- | --- | --- |
| 64 KB | `2¹⁶` | 太小 |
| 64 MB | `2²⁶` | 只算了 26 位字段，漏掉左移 2 位 |
| **256 MB** | `2²⁸` | **正确**：26 位字段左移 2 位后的范围 |
| 4 GB | `2³²` | 整个地址空间，`j` 无法覆盖 |

> 易错点：只看到「26 位」就得出 64 MB，忘了「地址 4 字节对齐、末两位恒为 0」这一前提。

> **并非所有指令都严格遵守上面三种格式**：有的如 `eret`、`syscall` 一样没有操作数；有的如 `jalr` 一样某些字段被固定为某个值。不过，就大部分指令而言，都可按上面三种格式进行解释，某些字段被固定也可以按照格式来识别为 R、I、J 中的一种，因此这三种格式要着重理解。

### 字段含义

- **op**：也称 opcode、**操作码**，用于标识指令的功能。CPU 需要通过这个字段来识别这是一条什么指令。不过，由于 `op` 只有 6 位，不足以表示所有的 MIPS 指令，因此在 R 型指令中，有 `funct` 字段来辅助它的功能。
- **funct**（也写作 `func`）：用于辅助 `op` 来识别指令。
- **rs**、**rt**、**rd**：通用寄存器的代号，并不特指某一寄存器，范围是 `$0 ~ $31`，用机器码表示就是 `00000 ~ 11111`。
- **shamt**：移位值，用于移位指令。
- **offset**：地址偏移量。
- **immediate**：立即数。
- **address**：跳转目标地址，用于跳转指令。

### 操作码（op）与 funct 速查

`op` 字段决定「这是哪一条（或哪一类）指令」。R 型的 `op` 恒为 `000000`，其他指令靠 `op`、R 型再靠 `funct` 区分。

| 指令 | op | op（二进制） | 类型 |
| --- | --- | --- | --- |
| R 型（`add`/`sub`/`and`/`or`/`slt`/`jr`…） | 0x00 | `000000` | 具体操作由 `funct` 区分 |
| `j` | 0x02 | `000010` | J 型 |
| `jal` | 0x03 | `000011` | J 型 |
| `beq` | 0x04 | `000100` | I 型，分支 |
| `bne` | 0x05 | `000101` | I 型，分支 |
| `blez` | 0x06 | `000110` | I 型，分支 |
| `bgtz` | 0x07 | `000111` | I 型，分支 |
| `addi` | 0x08 | `001000` | I 型 |
| `addiu` | 0x09 | `001001` | I 型 |
| `slti` | 0x0a | `001010` | I 型 |
| `sltiu` | 0x0b | `001011` | I 型 |
| `andi` | 0x0c | `001100` | I 型 |
| `ori` | 0x0d | `001101` | I 型 |
| `xori` | 0x0e | `001110` | I 型 |
| `lui` | 0x0f | `001111` | I 型 |
| `lb` | 0x20 | `100000` | I 型，取字节 |
| `lh` | 0x21 | `100001` | I 型，取半字 |
| `lw` | 0x23 | `100011` | I 型，取字 |
| `lbu` | 0x24 | `100100` | I 型 |
| `lhu` | 0x25 | `100101` | I 型 |
| `sb` | 0x28 | `101000` | I 型，存字节 |
| `sh` | 0x29 | `101001` | I 型，存半字 |
| `sw` | 0x2b | `101011` | I 型，存字 |

R 型指令的 `funct`（`op` 均为 0x00）：

| 指令 | funct | 指令 | funct |
| --- | --- | --- | --- |
| `sll` | 0x00 | `mult` | 0x18 |
| `srl` | 0x02 | `multu` | 0x19 |
| `sra` | 0x03 | `div` | 0x1a |
| `sllv` | 0x04 | `divu` | 0x1b |
| `srlv` | 0x06 | `add` | 0x20 |
| `srav` | 0x07 | `addu` | 0x21 |
| `jr` | 0x08 | `sub` | 0x22 |
| `jalr` | 0x09 | `subu` | 0x23 |
| `syscall` | 0x0c | `and` | 0x24 |
| `break` | 0x0d | `or` | 0x25 |
| `mfhi` | 0x10 | `xor` | 0x26 |
| `mthi` | 0x11 | `nor` | 0x27 |
| `mflo` | 0x12 | `slt` | 0x2a |
| `mtlo` | 0x13 | `sltu` | 0x2b |

> 记忆技巧：I 型指令的 `op` 按类别连续排列——条件分支 `0x04–0x07`、立即数运算 `0x08–0x0f`、取数集中在 `0x20` 段、存数集中在 `0x28` 段。

### 编码示例

寄存器编号见前文寄存器表：

```text
add $t0, $t1, $t2
= op(0) rs($t1=9) rt($t2=10) rd($t0=8) shamt(0) funct(0x20)
= 000000 01001 01010 01000 00000 100000
= 0x012A4020
```

```text
addi $t0, $t1, 100
= op(0x08) rs($t1=9) rt($t0=8) immediate(100=0x0064)
= 001000 01001 01000 0000000001100100
= 0x21280064
```

```text
sll $t0, $t1, 23
= op(0) rs(未用，填 0) rt($t1=9) rd($t0=8) shamt(23=0b10111) funct(0)
= 000000 00000 01001 01000 10111 000000
= 0x000945C0
```

### 编码综合例题

已知第一条指令的地址为 `0x00003000`：

```asm
ori  $t0, $0, 4
bne  $t0, $t1, next
nop

next:
sw   $t0, 4($t1)
```

**① `ori $t0, $0, 4`**（I 型；`rs` 位置放的是 `$0`）

```text
op = 0x0d = 001101      rs = $0  = 0 = 00000
rt = $t0 = 8 = 01000    immediate = 4 = 0000000000000100
= 001101 00000 01000 0000000000000100
= 0x34080004
```

**② `bne $t0, $t1, next`**（I 型；立即数是 **PC 相对偏移**，必须先算出地址）

```text
本指令地址 = 0x00003004，next 的地址 = 0x0000300c
偏移 = (next − (PC+4)) ÷ 4 = (0x0000300c − 0x00003008) ÷ 4 = 1

op = 0x05 = 000101      rs = $t0 = 8 = 01000
rt = $t1 = 9 = 01001    immediate = 1 = 0000000000000001
= 000101 01000 01001 0000000000000001
= 0x15090001
```

**③ `sw $t0, 4($t1)`**（I 型；`rs` 是基址寄存器，`rt` 是要存的数据）

```text
op = 0x2b = 101011      rs = $t1 = 9 = 01001
rt = $t0 = 8 = 01000    immediate = 4 = 0000000000000100
= 101011 01001 01000 0000000000000100
= 0xad280004
```

> 易错点：指令顺序存放，每条占 4 字节，故地址依次为 `0x3000`、`0x3004`、`0x3008`、`0x300c`；`bne` 的立即数字段存的是「以字为单位的相对偏移 1」，**不是** `next` 的地址。另外 `ori` 和 `sw` 里都出现了 4，但前者是立即数操作数、后者是地址偏移。

## 结合手册读懂指令

> 指令集手册里给出了**每一条指令的符号语言描述**。学有余力时可以逐条研究——它能把指令的作用和底层逻辑讲得比文字更准确，对之后用硬件描述语言实现指令（设计 CPU）很有帮助。

### 常用符号详解

- **`+`**：《MIPS-C 指令集》中的所有 `+` 一律将其左右两侧的操作数视为**无符号数**，再进行相加。
- **`-`**：《MIPS-C 指令集》中的所有 `-` 一律将其左右两侧的操作数视为**无符号数**，再进行相减。
- **下标**：变量的下标代表变量中所取用的位，`temp31` 即代表 `temp` 变量在二进制表示下的第 31 位。特别地，下标中的 `..` 可理解为「至」，`temp31..0` 与 `temp[31:0]` 是等价的。
- **上标**：某一位二进制数（即 0 或 1）的上标代表该数字重复的位数，`0^5` 即代表 `5'b00000`。
- **`||`**：该符号代表**拼接**操作，`temp1||temp2` 与 `{temp1,temp2}` 等价。
- **sign_extend()**：代表**符号扩展**，其扩展结果的正负会与被扩展数相同，一般用于有符号数。扩展方法是：将被扩展数不足的部分用被扩展数的**符号位**补全——即若被扩展数为正数，则将其高位用 0 补全至相应位数；若为负数，则在高位补 1 至相应位数。
- **zero_extend()**：代表**无符号扩展**，即倘若被扩展数位数不足，则将其高位补 0 至相应位数。
- **GPR**：即 General Purpose Register（通用寄存器），其后的中括号内为寄存器编号，`GPR[rs]` 即可表示编号为 `rs` 的寄存器。
- **memory**：表示内存，当其后跟随中括号时，表示存储在以括号中数值为起始地址的 **4 字节**内存中的数据。以 `memory[Addr]` 为例，其表示以 `Addr` 为首地址的 4 字节内存中存储的数据。

### 两种扩展的对比

| 函数 | 高位补什么 | 常见用于 |
| --- | --- | --- |
| `sign_extend()` | 原数的**符号位** | 有符号数：`addi` 的立即数、分支指令的字偏移 |
| `zero_extend()` | 全 0 | 无符号数：`andi`、`ori` 的立即数 |

## 立即数的处理方式

I 型指令的 16 位立即数（分支指令中称为 `offset`）在不同指令里有不同的语义，硬件需要根据 `op` 来决定如何加工它。结合《MIPS-C 指令集》，共有下面四种处理方式。

### 拼接两位 0：分支指令

| 指令 | 说明 |
| --- | --- |
| `beq`、`bne` | 相等 / 不等则分支 |
| `blez`、`bgtz` | 与 0 比较后分支 |
| `bltz`、`bgez`、`bltzal`、`bgezal` | 其他条件分支 |

```text
offset(16 位) || 00
```

拼接两位 0 后，数值相当于 `offset × 4`，即把「**字**偏移」还原为「**字节**偏移」。

> **注意**：这是**低位**补 0（相当于左移 2 位），与「零扩展」的**高位**补 0 完全不同。

### 符号扩展：算术立即数、访存偏移、分支偏移

| 类别 | 指令 |
| --- | --- |
| 算术立即数 | `addi`、`addiu`、`slti`、`sltiu` |
| 访存偏移 | `lw`、`sw`、`lh`、`lhu`、`sh`、`lb`、`lbu`、`sb` |
| 分支偏移 | `beq`、`bne`、`blez`、`bgtz` 等（拼接两位 0 之后仍要符号扩展） |

### 零扩展：逻辑立即数

| 指令 | 备注 |
| --- | --- |
| `andi`、`ori`、`xori` | 逻辑运算，高位补 0，取值区间为 `0 ~ 65535` |

### 特例：`lui`

`lui` 既不做符号扩展也不做零扩展，而是把 16 位立即数直接放到结果的**高 16 位**、低 16 位清 0：

```text
lui rt, imm  ⇒  GPR[rt] ← imm(16 位) || 0000000000000000
```

**小例**：手册读法可以直接套用

```text
lui $t0, 0x1234  ⇒  GPR[t0] ← 0x1234 || 0^16 = 0x12340000
```

### 为什么要分成这几种

关键在于：**同一个 16 位字段，在不同指令中有不同语义**。

| 处理方式 | 代表的含义 | 为什么这样选 |
| --- | --- | --- |
| 拼接两位 0 | 偏移量以「字」为单位 | 指令地址必然 4 字节对齐，低 2 位恒为 0，省下 2 位可让分支范围扩大到 ±128 KB |
| 符号扩展 | 该数**可能是负数** | `addi $t0,$t0,-1`、`lw $t0,-4($sp)`、向后跳的分支都要表示负数 |
| 零扩展 | 该数只当作**位模式** | 逻辑运算关心的是位：若把 `0xFFFF` 符号扩展成 `0xFFFFFFFF`，`ori` 就没法只置低 16 位为 1 了 |
| 放到高 16 位 | 专门用于构造常数的高半部分 | `lui` + `ori` 组合出 32 位常数 |

> 一句话：**算术 / 访存 / 分支要正负 → 符号扩展；逻辑运算要看位 → 零扩展；目标是地址 → 先拼两位 0。**

### 手册中的典型写法

```text
addi $t1, $t2, imm16
  ⇒ GPR[t1] ← GPR[t2] + sign_extend(imm16)

ori  $t1, $t2, imm16
  ⇒ GPR[t1] ← GPR[t2] OR zero_extend(imm16)

lw   $t1, offset16($t2)
  ⇒ Addr ← GPR[t2] + sign_extend(offset16)

beq  $t1, $t2, offset16
  ⇒ if GPR[t1] == GPR[t2] then
        PC ← PC + 4 + sign_extend(offset16 || 0^2)
```

> 注意最后一行：**分支指令同时做了两件事**——先拼两位 0，再符号扩展。两种等价写法（先拼接后扩展 / 先符号扩展再左移 2 位）算出的结果相同。

**另外两条常用指令的伪码**：

```text
sb   $t1, offset16($t2)
  ⇒ Addr ← GPR[t2] + sign_extend(offset16)
    byte ← Addr1..0                    # 地址末两位，决定落在字内哪个字节
    memory[Addr]7+8×byte..8×byte ← GPR[t1]7..0

jal  label
  ⇒ PC ← PC31..28 || instr_index || 0^2
    GPR[31] ← PC + 4
```

### 易错点

| 坑 | 正确理解 |
| --- | --- |
| `addiu` 里带 `u`，是不是零扩展？ | **不是**。`u` 表示「**不产生溢出异常**」，立即数**仍然符号扩展** |
| `sltiu` 是否零扩展？ | 同上，立即数**符号扩展**；`u` 只是说比较按无符号数语义 |
| `lbu`、`lhu` 的 `u` 呢？ | 这里的 `u` 指**取回数据后不做符号扩展**（即零扩展装入寄存器）；而它的**地址偏移依然符号扩展**。两个 `u` 含义完全不同 |
| 分支是不是「零扩展」？ | **不是**。零扩展在**高位**补 0，分支是在**低位**补 0 |
| `j`、`jal` 算拼接两位 0 吗？ | J 型指令的 26 位字段是**地址**而不是「立即数」，但其目标地址构造同样是 `{PC+4 的高 4 位, address, 00}`，**末尾也要补两位 0**。若题目明确说「立即数」，则不包含 `j`/`jal` |
| 分支偏移相对谁？ | 相对 **`PC+4`**（即下一条指令地址），且单位是**字**（已除过 4），不是字节 |

### 速记口诀

```text
带 i 的算术指令  → 符号扩展                （addi / slti ...）
带 i 的逻辑指令  → 零扩展                  （andi / ori / xori）
访存指令的偏移   → 符号扩展                （lw / sw / lb / sb ...）
分支指令的偏移   → 拼两位 0 + 符号扩展     （beq / bne / blez / bgtz）
lui              → 高 16 位，低 16 位清零
```

## 寻址方式

指令如何得到操作数的位置，称为**寻址方式**。MIPS 常用的寻址方式如下：

| 寻址方式 | 形式 | 例子 | 说明 |
| --- | --- | --- | --- |
| 立即数寻址 | `imm` | `addi $t0, $t1, 100` | 操作数直接写在指令中 |
| 寄存器寻址 | `$rs` | `add $t0, $t1, $t2` | 操作数在寄存器中 |
| 基址寻址 | `offset($rs)` | `lw $t0, 4($sp)` | 地址 = 基址寄存器 + 16 位偏移 |
| PC 相对寻址 | `Label` | `beq $t0, $t1, loop` | 地址 = `PC+4` + 偏移量 × 4 |
| 伪直接寻址 | `Label` | `j loop` | 地址 = `PC+4` 的高 4 位拼接（26 位字段 × 4） |

存取类指令 `lw`、`sw`、`lb`、`sb` 使用的就是形如 `offset($rs)` 的基址寻址，即前文写作 `操作数1, 操作数3(操作数2)` 的形式。

**一行一个例子**：

```asm
addi $t0, $t1, 100     # 立即数寻址：100 直接写在指令里
add  $t0, $t1, $t2     # 寄存器寻址：两个操作数都在寄存器中
lw   $t0, 4($sp)       # 基址寻址：地址 = $sp 的值 + 4
beq  $t0, $t1, loop    # PC 相对寻址：标签会被换成相对 PC+4 的字偏移
j    loop              # 伪直接寻址：标签会被换成 26 位地址字段
```

## 访存类指令

先给出访存指令速查表，再讨论地址构造、非法与未对齐地址，以及 `sh`/`lh`/`sb`/`lb` 对寄存器中数据的影响，最后用一个完整的 load-store 例题收尾。

### 访存（I 型）

| 指令 | 全称 | 含义 |
| --- | --- | --- |
| `lw` | **L**oad **W**ord | 取字（4 字节） |
| `lh` | **L**oad **H**alfword | 取半字（符号扩展） |
| `lhu` | **L**oad **H**alfword **U**nsigned | 取半字（零扩展） |
| `lb` | **L**oad **B**yte | 取字节（符号扩展） |
| `lbu` | **L**oad **B**yte **U**nsigned | 取字节（零扩展） |
| `lwl`、`lwr` | **L**oad **W**ord **L**eft / **R**ight | 未对齐取字（左半 / 右半） |
| `sw` | **S**tore **W**ord | 存字 |
| `sh` | **S**tore **H**alfword | 存半字 |
| `sb` | **S**tore **B**yte | 存字节 |
| `swl`、`swr` | **S**tore **W**ord **L**eft / **R**ight | 未对齐存字 |
| `ll` | **L**oad **L**inked | 取字并建立链接（原子操作） |
| `sc` | **S**tore **C**onditional | 条件存字（链接成功才写入） |

**小例**：把 1 存进内存再取回

```asm
li  $t1, 1
sw  $t1, 0($sp)    # 把 $t1 的 32 位值写入 $sp 指向的那个字
lw  $t2, 0($sp)    # 从同一地址取回，$t2 = 1
sh  $t1, 4($sp)    # 按半字存入：只写低 16 位
lbu $t3, 4($sp)    # 按字节取回并零扩展，$t3 = 0x00000001
```

### `sw`/`lw` 写地址时的经典易错点

用一块连续内存实现 `int` 类型的数组 `arr[]`，若 `arr[0]` 的地址是标签 `arr`，要把 `$t1` 的内容存入 `arr[3]` 该怎么写？

`int` 占 32 位，每个元素占 4 个地址，所以 `arr[3]` 的地址是 `arr + 12`。于是很多初学者顺手写成：

```asm
sw $t1, 12(arr)   # 错误！
```

**正确做法**是先把偏移量放进寄存器，再用寄存器寻址：

```asm
li $t0, 12
sw $t1, arr($t0)  # 正确
```

> **原因**：`sw $t1, label($t0)` 中的 `label` 会被替换到**立即数 `imm16` 的位置**，而**不是括号里的寄存器位置**（回想 `la $t0, label`：`label` 也是被替换成立即数）。所以括号里只能放寄存器，不能写「数字 + 标签」。（有点类似 C 语言里 `arr[3]` 等价于 `3[arr]`，很值得记牢。）

### 非法地址与未对齐

计算出要访问的地址后：

- 若该地址**不存在或非法**（例如为负），会直接报错、抛出异常；
- 若地址**没有按要求对齐**（字访问非 4 的倍数、半字访问非 2 的倍数），同样报错、抛出异常。

**小例**（设 `$s0 = 0x00001000`）：

```asm
lw $t0, 2($s0)    # 地址 0x1002 不是 4 的倍数 → 报错（未对齐）
lh $t0, 1($s0)    # 地址 0x1001 不是 2 的倍数 → 报错（未对齐）
lb $t0, 1($s0)    # 字节访问无对齐要求 → 正常执行
sw $t0, -8($0)    # 地址 = 0 + (−8) = −8 → 非法地址，报错
```

### 两道容易搞混的判断题

| 问题 | 答案 | 原因 |
| --- | --- | --- |
| `lw $t0, 0($sp)` 与 `move $t0, $sp` 等价吗？ | **不等价** | 前者把 `$sp` 里的值当作**地址**，去内存中找到数据再赋给 `$t0`；后者直接把 `$sp` 的值赋给 `$t0` |
| `lw $t0, label($0)` 与 `la $t0, label` 等价吗？ | **不等价** | 前者取的是**内存中**该地址处的数据；后者把**地址本身**赋给 `$t0` |

> 一句话：**访存指令读的是「内存里的内容」，`la` 得到的是「地址本身」。**

### `sh`/`lh`/`sb`/`lb` 对寄存器的影响

| 指令 | 对数据宽度的处理 |
| --- | --- |
| `sb $t1, imm16($t0)` | 寄存器有 32 位而只写 8 位，于是**抛弃高出来的位，只把最低 8 位写入内存**（如 `$t1 = 0x12345678` 时只写入 `0x78`） |
| `sh` | 同理，只把最低 16 位写入内存 |
| `lb $t1, imm16($t0)` | 取回的字节放在寄存器的**最低位**，然后**符号扩展**至 32 位：如取到 `0xcd`，结果是 `0xffffffcd` |
| `lh` | 同理，取回半字后符号扩展至 32 位 |

**小例**：内存中某个字节是 `0xcd`，寄存器 `$t1` 原值为 `0x12345678`

| 指令 | 执行后 |
| --- | --- |
| `lb $t1, 0($t0)` | `$t1 = 0xffffffcd`（取回字节后**符号扩展**，高位补 1） |
| `lbu $t1, 0($t0)` | `$t1 = 0x000000cd`（取回字节后**零扩展**，高位补 0） |
| `sb $t1, 0($t0)` | `$t1` 不变，内存中只写入最低字节 `0x78` |

### 例题：load-store 指令与小端存取

在小端存储的 CPU 中执行下列指令，求执行后 `$t0`、`$t1`、`$t2` 的值：

```asm
li  $a0, 0x12345678
li  $s0, 0x00001000

sw  $a0, 0($s0)      # 存字
lb  $t0, 1($s0)      # 取字节（符号扩展）
sb  $a0, 2($s0)      # 存字节（只写最低 8 位）

lw  $t1, 0($s0)      # 取字
sh  $a0, 2($s0)      # 存半字（只写最低 16 位）
lw  $t2, 0($s0)      # 再取字
```

**① `sw $a0, 0($s0)`：小端存字**

小端把最低有效字节放在最低地址：

| 地址 | 内容 | 说明 |
| --- | --- | --- |
| `0x1000` | `0x78` | 最低字节 LSB |
| `0x1001` | `0x56` | |
| `0x1002` | `0x34` | |
| `0x1003` | `0x12` | 最高字节 MSB |

**② `lb $t0, 1($s0)`：取字节并符号扩展**

读 `0x1001` 的 1 字节 = `0x56`，最高位为 0（正数），高位补 0：

```text
0x56 → 0x00000056
```

**③ `sb $a0, 2($s0)`：只写最低字节**

把 `$a0` 的最低 8 位（`0x78`）写入 `0x1002`，其余字节不变。

**④ `lw $t1, 0($s0)`：取字**

此时 `0x1000`～`0x1003` 为 `78 56 78 12`，按小端还原：

```text
0x78 + 0x56×2⁸ + 0x78×2¹⁶ + 0x12×2²⁴
= 0x78 + 0x5600 + 0x780000 + 0x12000000
= 0x12785678
```

**⑤ `sh $a0, 2($s0)`：只写最低半字**

把 `$a0` 的最低 16 位（`0x5678`）写入 `0x1002`～`0x1003`：低字节 `0x78` → `0x1002`，高字节 `0x56` → `0x1003`。

**⑥ `lw $t2, 0($s0)`：再取字**

```text
0x78 + 0x56×2⁸ + 0x78×2¹⁶ + 0x56×2²⁴
= 0x78 + 0x5600 + 0x780000 + 0x56000000
= 0x56785678
```

**内存变化总览**

| 地址 | `sw` 之后 | `sb` 之后 | `sh` 之后 |
| --- | --- | --- | --- |
| `0x1000` | `0x78` | `0x78` | `0x78` |
| `0x1001` | `0x56` | `0x56` | `0x56` |
| `0x1002` | `0x34` | **`0x78`** | `0x78` |
| `0x1003` | `0x12` | `0x12` | **`0x56`** |

**答案**

| 寄存器 | 值 | 由来 |
| --- | --- | --- |
| `$t0` | `0x00000056` | `lb` 取 `0x1001` 的 `0x56`，符号扩展后不变 |
| `$t1` | `0x12785678` | 读 `0x1000`～`0x1003` = `78 56 78 12` |
| `$t2` | `0x56785678` | `sh` 改掉 `0x1003` 后重新取字 |

**易错点**

- **`sw`/`sh`/`sb` 只写自己宽度的字节**：`sw` 写 4 字节、`sh` 写 2 字节、`sb` 写 1 字节，其余字节保持原值——这正是 `$t1 ≠ $t2` 的原因。
- **`lb` 是符号扩展的**：若取到的字节是 `0x80`～`0xff`（最高位为 1），结果会变成 `0xffffff××`；要零扩展得用 `lbu`。
- **小端的影响**：多字节数据的低字节放在低地址，所以「先存的低字节」在 `lw` 回来时是数值的低位。
- 本题的地址 `0x1000` 只是题设；MARS 中数据段默认从 `0x10010000` 开始，实际调试时用 `.data` 里的标签更稳妥（`lw`/`sw` 还要求地址 4 字节对齐）。

## 跳转类指令

先给出分支与跳转指令速查表，再讨论 `j`/`jr`/`jalr` 的细节、分支标签的翻译方式与跳转范围。

### 分支与跳转

| 指令 | 全称 | 含义 |
| --- | --- | --- |
| `beq` | **B**ranch if **EQ**ual | 相等则分支 |
| `bne` | **B**ranch if **N**ot **E**qual | 不相等则分支 |
| `blez` | **B**ranch if **L**ess than or **E**qual to **Z**ero | ≤0 则分支 |
| `bgtz` | **B**ranch if **G**reater **T**han **Z**ero | >0 则分支 |
| `bltz` | **B**ranch if **L**ess **T**han **Z**ero | <0 则分支 |
| `bgez` | **B**ranch if **G**reater than or **E**qual to **Z**ero | ≥0 则分支 |
| `bltzal` | **B**ranch if **L**ess **T**han **Z**ero **A**nd **L**ink | <0 则分支并保存返回地址 |
| `bgezal` | **B**ranch if **G**reater than or **E**qual to **Z**ero **A**nd **L**ink | ≥0 则分支并保存返回地址 |
| `j` | **J**ump | 无条件跳转 |
| `jal` | **J**ump **A**nd **L**ink | 跳转并把返回地址存入 `$ra` |
| `jr` | **J**ump **R**egister | 跳转到寄存器中的地址 |
| `jalr` | **J**ump **A**nd **L**ink **R**egister | 寄存器跳转并保存返回地址 |

**小例**：`$t0 == $t1` 时跳过一条指令

```asm
beq  $t0, $t1, skip    # 相等 → 直接跳到 skip
addi $s0, $s0, 1       # 不相等时才执行这一条
skip:
    addi $s1, $s1, 1
```

### `j`：只能跟标签

`j` 的标签在汇编后会被替换成**目标指令的地址**（如 `0x00003008`），因此 `j` 也可以理解为「跳到某个绝对地址」。

> MARS 中 `j` **不允许使用立即数**，必须强制使用标签——大概是因为「允许用户想跳到哪就跳到哪」太危险了。

### `jr`：不一定要和 `jal` 搭配

`jr` 的作用只是「把寄存器中的值当作地址跳过去」，所以：

- 理论上 `jr` 后面可以接**任何寄存器**，并不一定要与 `jal`、`$ra` 搭配使用；
- 因此它可以**变相实现跳转到任意地址**（如 `la $at, label` + `jr $at`）。

**小例**：一条「调用 → 返回」的完整路径

```asm
    jal  func          # 跳转，并把下一条指令的地址写入 $ra
    addi $t0, $t0, 1   # 返回后从这里继续执行
    ...
func:
    addi $t1, $t1, 1
    jr   $ra           # 回到 $ra 指向的那条指令
```

### 为什么 `$ra` 存的是「下一条指令」而不是 `jal` 本身？

`jal label` 会把 **PC + 4**（下一条指令的地址）写入 `$ra`。如果保存的是 `jal` 自身的地址，那么 `jr $ra` 返回时就会**再次执行这条 `jal`**，从而陷入**死循环**。

> 因此，从 `jal` 跳转到 `jr` 返回的整个过程中（其实任何时候都是）**不要篡改 `$ra`**，否则会跳到乱七八糟的地方。

### `jalr`：两个寄存器不能相同

`jalr $t1, $t0` 表示「跳转到 `$t0` 中的值所代表的地址，并把 **PC + 4** 写入 `$t1`」。

> **两个寄存器不能相同**：`jalr` 执行时是**先写 PC + 4、再执行跳转**；若两个寄存器相同，跳转用的地址会在跳转前就被返回地址**覆盖**掉，于是根本没法跳转。

**小例**：

```asm
jalr $t1, $t0   # 正确：跳到 $t0 里的地址，并把返回地址写入 $t1
jalr $t0, $t0   # 错误：PC + 4 先覆盖了 $t0，目标地址丢失，无法跳转
```

### 条件分支家族：只有 6 条是「真指令」

一口气能数出 16 条 b 家族指令，但其中**只有 6 条是真指令**：`beq`、`bne`、`bgtz`、`bgez`、`bltz`、`blez`。

- 其余都是伪指令：`bgt`、`bge`、`blt`、`ble` 及其无符号版本、`beqz`、`bnez` 等；
- 其实并不需要刻意区分，用起来都一样，只是设计 CPU 时不需要考虑伪指令。

**无符号比较的变种**：`bgt`、`bge`、`blt`、`ble` 都是**有符号**比较（认为 `0x1` 大于 `0xffffffff`），因此还有四条无符号版本：

| 有符号 | `bgt` | `bge` | `blt` | `ble` |
| --- | --- | --- | --- | --- |
| 无符号 | `bgtu` | `bgeu` | `bltu` | `bleu` |

**与 0 比较有更简单的写法**：把第二个寄存器换成 `$0` 是一种办法；更简单的是在 `beq`、`bne`、`bgt`、`bge`、`blt`、`ble` 后面直接加字母 `z`，变成 `beqz`、`bnez`、`bgtz`、`bgez`、`bltz`、`blez`，同时省掉第二个寄存器参数。

```asm
bgez $t0, label   # 若 $t0 中的值 ≥ 0，则跳转到 label
```

> **为什么 `bgtu`/`bgeu`/`bltu`/`bleu` 后面不能加 `z`？** 因为无符号数与 0 比较，结果不是恒真就是恒假，加了 `z` 也没有意义——想通这一点会觉得很好笑。

**小例**：下面两种写法完全等价

```asm
beq  $t0, $0, exit   # 与 $0 比较：相等则跳转
beqz $t0, exit       # 伪指令写法，更简洁
```

### 分支标签被翻译成「相对条数」

分支指令的标签**不是**翻译成目标地址，而是翻译成「目标指令」与「当前分支指令的下一条指令」相差的**指令条数**：

```asm
li  $s0, 1
li  $s1, 1
beq $s0, $s1, target3
li  $t6, 7
target3:
li  $t7, 8
```

| 标签指向的指令 | 翻译结果 | 含义 |
| --- | --- | --- |
| 分支指令后面的**第二条**（上例） | `0x00000001` | 跳过 1 条指令 |
| 分支指令的**下一条** | `0x00000000` | 与不跳转结果相同 |
| 分支指令**自身** | `0xffffffff`（即 −1） | 回头再执行自己 |

> 注意单位是**条数**而不是地址：上例中翻译结果是 `0x00000001`，**不是** `0x00000004`。这也正是 `j` 型被称为「绝对跳转指令」、`b` 型被称为「相对跳转指令」的原因。

### 跳转与分支的范围对比

不同跳转方式的「目标地址来源」不同，能覆盖的地址范围差别很大：

| 指令 | 类型 | 目标地址来源 | 可跳范围 | 限制 |
| --- | --- | --- | --- | --- |
| `beq`、`bne`、`blez`、`bgtz` 等分支 | I 型 | 16 位有符号偏移 × 4，再加到 `PC+4` | **±128 KB** | 只能跳到附近 |
| `j`、`jal` | J 型 | 26 位字段 × 4，拼接 `PC+4` 的高 4 位 | **256 MB** | 必须与 `PC+4` 处于同一 256 MB 区域 |
| `jr`、`jalr` | R 型 | 32 位通用寄存器的完整值 | **整个 4 GB** | 无区域限制 |

- **分支**：偏移字段 16 位且以**字**为单位，故 `±32768 字 × 4 字节 = ±128 KB`。
- **`j`/`jal`**：26 位字段左移 2 位后是 28 位，`2²⁸` 字节 = 256 MB（推导见前文「J 型指令」）。
- **`jr`/`jalr`**：地址直接取自 32 位寄存器，可覆盖 `2³²` 字节 = 4 GB 的整个空间——这正是函数返回（`jr $ra`）与远距离跳转（`la $at, label` + `jr $at`）都使用它的原因。

> 记忆：**偏移越短、范围越小；地址越完整、范围越大。**

## 编程实战：从 C 到 MIPS

前面学的都是零散的指令，这一章把它们串起来：用分支、循环、数组、字符串、系统调用、宏、函数与栈，写出与 C 程序等价的 MIPS 程序。

### if 分支

```c
if (a > b) { a = 0; }
else       { b = 0; }
c = 0;
```

设 `$s0 = a`、`$s1 = b`、`$s2 = c`，翻译为：

```asm
        bgt   $s0, $s1, branch1   # a > b 则跳到 branch1
        j     branch2             # 否则走 else 分支
branch1:
        li    $s0, 0              # a = 0
        j     end                 # 别忘了：跳过 else 部分
branch2:
        li    $s1, 0              # b = 0
end:
        li    $s2, 0              # c = 0
```

- if → **b 型跳转**指向 then 分支；else → **j 型跳转**指向 else 分支；
- then 分支末尾**必须**补一条 `j end`，否则执行完 `li $s0, 0` 会接着执行 else 的内容；
- `#` 是单行注释，相当于 C 的 `//`。

**else if 链**：再加一条 b 型跳转即可，每个分支末尾同样 `j end`：

```asm
        bgt $s0, $s1, branch1     # if (a > b)
        blt $s0, $s1, branch2     # else if (a < b)
        j   branch3               # else
branch1:  li $s0, 0
          j  end
branch2:  li $s1, 0
          j  end
branch3:  li $s0, 0
          li $s1, 0
end:      li $s2, 0
```

> **代码风格建议**：汇编本来就很抽象，建议像 C 一样缩进，并给变量赋值、分支意图多写注释，否则上百行以后很快就会把自己绕进去。

### 逻辑或与逻辑与

**`||`：连续两条条件跳转指向同一个分支即可**

```c
if (a > b || b > c) { a = 0; } else { b = 0; }
```

```asm
        bgt $s0, $s1, branch1   # a > b
        bgt $s1, $s2, branch1   # 或者 b > c
        j   branch2             # 两个都不满足 → else
branch1:
        li  $s0, 0
        j   end
branch2:
        li  $s1, 0
end:
        li  $s2, 0
```

**`&&`：两种办法**

1. **条件取反、交换两支**（只适合只有 if/else 两支的情况）：`if (a > b && b > c) { a = 0; } else { b = 0; }` 改写成 `if (a <= b || b <= c) { b = 0; } else { a = 0; }`，就又能套用 `||` 的写法；一旦还有 else if，这种办法就力不从心了。
2. **嵌套 if**：在外层 then 分支里再写一套 if：

```asm
        bgt $s0, $s1, branch1   # if (a > b) { ... } else { b = 0; }
        j   branch2
branch1:
        bgt $s1, $s2, branch3   #     if (b > c) { a = 0; }
        j   branch2             #     else { b = 0; }
branch3:
        li  $s0, 0
        j   end
branch2:
        li  $s1, 0
end:
        li  $s2, 0
```

### while 循环的两种写法

C 例子（`$s0 = a`、`$s1 = b`、`$s2 = 5`）：`a = 0; while (a < 5) { b = a; a++; } a = 0;`

**写法一：进入循环时条件跳转，跳出循环时直接跳转**

```asm
        li   $s0, 0
        li   $s2, 5
while:
        blt  $s0, $s2, loop     # a < 5 才进入循环体
        j    end
loop:
        move $s1, $s0           # b = a
        addi $s0, $s0, 1        # a++
        j    while
end:
        li   $s0, 0
```

- 优点：循环条件与 `||` 结合容易（再加一条跳到 `loop` 的条件跳转即可）；
- 缺点：循环条件里出现 `&&` 时写起来麻烦。

**写法二：进入循环不跳转，跳出循环时条件跳转**

```asm
        li   $s0, 0
        li   $s2, 5
while:
        bge  $s0, $s2, end      # 条件取反：a >= 5 就跳出
        move $s1, $s0           # b = a
        addi $s0, $s0, 1        # a++
        j    while
end:
        li   $s0, 0
```

- 优点：代码更短、结构更像 C 的 while，且循环条件与 `&&` 结合容易（再加一条跳到 `end` 的条件跳转即可）；
- 缺点：判断条件要**取反**（`a < 5` → `bge … end`），条件一复杂就容易写错。

### for 循环：拆成 while

`for` 就是特殊的 `while`，改写三步走：

1. 把 `for` 括号里的**初始化**提到循环前面；
2. 把**更新表达式**放到循环体最后；
3. 把 `for` 改成 `while`。

例如 `for (a = 0; a < 5; a++) b = a;` 等价于 `a = 0; while (a < 5) { b = a; a++; }`，再套用上面的 while 写法即可。

> **例**：判断质数的代码（`for (int i = 3; i * i <= n; i += 2)`）与「先 `int i = 3;`，再 `while (i * i <= n) { … i += 2; }`」完全等价——`i += 2` 就是被搬到循环体末尾的更新表达式。

### 一维数组

`.data` 段（`0x00000000`~`0x00002fff`）可以随意用来存数据，`.text` 段（`0x00003000` 之后）存指令机器码。用 `.space` 在 `.data` 段开辟连续空间，就能当数组用：

```asm
.data
array1: .space 20        # 20 字节 = 5 个字 → array1[0] ~ array1[4]
array2: .space 40        # 40 字节 = 10 个字 → array2[0] ~ array2[9]

.text
        li  $s0, 1
        li  $s1, 2
        li  $t0, 0
        li  $t1, 4
        sw  $s0, array1($t0)   # array1[0] = $s0
        sw  $s1, array1($t1)   # array1[1] = $s1
        sw  $s0, array2($t0)   # array2[0] = $s0
        sw  $s1, array2($t1)   # array2[1] = $s1
```

要点：

- 每个元素按 **4 字节**存（即使 `char` 也建议用 4 字节：与寄存器同宽、可直接 `sw`/`lw`、不容易出错），所以 `.space 20` 只能放 **5** 个元素；
- 数组名是**标签**，运行时被替换成该空间的首地址：`array1` → `0x00000000`，`array2` → `0x00000014`（十进制 20，因为 array1 占了 `0x0`~`0x13`）；
- **申请字节数一定要是 4 的倍数**：否则后面数组的首地址也不再是 4 的倍数，`sw` 一访问就会因「地址未对齐」直接报错崩溃；
- 括号里必须是**寄存器**（不能写立即数）；寄存器里存的不是下标，而是**下标 × 4**，用 `sll $t1, $t0, 2` 一步就能乘 4；
- 在 `.data` 段同时声明数组和字符串时，**先声明数组、再声明字符串**（理由同上）。

### 二维数组

二维数组在内存中是**按行展平**存储的：m 行 n 列的 `matrix[i][j]` 等价于一维的 `matrix[i * n + j]`，所以

```text
括号中寄存器的值 = (i * n + j) × 4
```

3 行 5 列的 `matrix` 需要 `3 × 5 × 4 = 60` 字节（自己写时可以多开一点，防止算错空间越界）。写 `matrix[1][2]` 的过程：

```asm
.data
matrix: .space 60

.text
        li   $s0, 3
        li   $s1, 5            # n = 5
        li   $t0, 1            # i = 1
        li   $t1, 2            # j = 2
        li   $t3, 0x114514
        mult $t0, $s1          # LO = i * n
        mflo $t2
        add  $t2, $t2, $t1     # i * n + j
        sll  $t2, $t2, 2       # × 4
        sw   $t3, matrix($t2)  # matrix[1][2] = $t3
```

**优化技巧**：把列数取成 2 的幂（例如用 `matrix[3][8]`，96 字节），第 0~4 列正常使用、多余列不管，就可以用 `sll` 一步算出偏移，不必再 `mult`。这类偏移计算也可以写成**宏**（见后文「宏定义」）。

### 字符与字符串

- 字符用 **ASCII 码**存：常用字符只需 7 位，但为方便用 **1 字节**存一个字符（与 C 的 `char` 相同）；ASCII 码 128~255 是键盘上打不出来的特殊字符；
- **易错**：十六进制 ASCII 码和字符本身「长得一样」。`0x0000000b` 是空白符 `\v`（什么也打印不出来），字符 `b` 其实要写成 `0x00000062`；
- 字符串放在内存里（一个寄存器最多装 4 个字符）：

```asm
.data
str1: .ascii  "Hello, "        # 末尾不加 \0
str2: .asciiz "World!"         # 末尾自动补 \0
str3: .asciiz "I'm Kamonto!"
```

- **`.ascii` 与 `.asciiz` 的区别就在结尾的 `\0`**：没有 `\0` 时，字符串会与后面的内容连成一串，直到遇到 `\0` 为止；正常写程序就用 `.asciiz`；
- 在内存界面点 **ASCII** 按钮可切换成字符显示：同一个字内的 4 个字节「从左往右地址递减」，字与字之间「从左往右地址递增」（小端的体现），按地址从低到高读出来才是正确顺序。

### syscall：输入、输出与退出

汇编本身没法输入输出，必须**系统调用**：用 `$v0` 的值告诉操作系统要做什么（MARS 中按 **F1** → *Syscalls* → *Table of Available Services* 可查全部）。常用调用：

| `$v0` | 功能 | 参数 | 结果 |
| --- | --- | --- | --- |
| 1 | 打印整数 | `$a0` = 要打印的整数 | — |
| 4 | 打印字符串 | `$a0` = 字符串**首地址**（以 `\0` 结尾） | — |
| 5 | 读入整数 | — | 结果在 `$v0` |
| 10 | 结束程序 | — | — |
| 11 | 打印字符 | `$a0` = 字符（**只有最低 8 位有效**） | — |
| 12 | 读入字符 | — | 结果在 `$v0` |

```asm
        li  $a0, 114514        # 要输出的整数
        li  $v0, 1             # 1 号调用：打印整数
        syscall                # 输出 114514

        li  $v0, 5             # 5 号调用：读入整数
        syscall                # 输入 114514 后，$v0 = 0x0001bf52
```

要点：

- **输出看 `$a0`，输入结果看 `$v0`**，两者容易记混；
- 输入后 `$v0` 已被结果占据，**下一次输入前必须重新给 `$v0` 赋值**；
- 打印字符串时 `$a0` 不是字符串本身而是**首地址**，用 `la $a0, label` 装入；输出从首地址一直进行到第一个 `\0` 为止；
- 常见技巧：把单个字符也当字符串存（`space: .asciiz " "`、`enter: .asciiz "\n"`），用 4 号调用输出，省下寄存器；
- 程序结尾建议统一加 `li $v0, 10` + `syscall` 结束运行。

**字符串输出小例**（`str1` 是 `.ascii "Hello, "`，`str2` 是 `.asciiz "World!"`）：

```asm
        li   $v0, 4
        la   $a0, str1
        syscall                # 输出 Hello, World!（str1 没有 \0，会接着输出 str2）
        addi $a0, $a0, 1
        syscall                # 输出 ello, World!（从 e 开始）
        la   $a0, str2
        syscall                # 输出 World!（str2 自带 \0，到此为止）
```

### 宏定义

`.macro` 与 `.end_macro` 可以把一段指令打包成一个「新指令」，减少重复：

```asm
.macro exit              # 无参数时括号可省略
        li   $v0, 10
        syscall
.end_macro

        ori  $t0, $t0, 1
        exit             # 相当于 li $v0,10 + syscall
        ori  $t1, $t1, 2
```

**带参数**：参数名前面加 `%`：

```asm
.macro push(%d)          # 压栈
        addi $sp, $sp, -4
        sw   %d, 0($sp)
.end_macro

.macro pop(%d)           # 弹栈
        lw   %d, 0($sp)
        addi $sp, $sp, 4
.end_macro

        push($t0)        # 展开为 addi $sp,$sp,-4 与 sw $t0,0($sp)
        push($t1)
```

常见用途与特性：

- 打包输入输出：`scanfint(%addr)`（`li $v0,5` + `syscall` + `move %addr,$v0`）、`printfint(%addr)`（`move $a0,%addr` + `li $v0,1` + `syscall`）；
- **多参数**（逗号分隔）：`getoffset(%ans, %i, %j)` 计算「每行 8 列」二维数组的偏移：`%ans = (%i * 8 + %j) * 4`；
- 参数可以代表指令的**任意部分**：立即数（`para(4)`）、标签（`label(label1)`）、甚至**指令名**（`instr(add)`）；
- 宏内部可以跳转（如 `beqz %d, iszero`），MARS 展开时会自动给内部标签**加后缀**区分，所以同一个宏可以被调用多次；
- 宏可以**嵌套调用**，但不能自己调用自己或循环调用（会无法展开）；也不能从宏外跳进宏内、或从宏内跳到宏外。

### 函数：jal、jr 与参数约定

**简单函数优先直接展开**。例如交换两个变量 `swap(&x, &y)`，可以直接展开成三条 `xor`：`xor $s0,$s0,$s1` / `xor $s1,$s0,$s1` / `xor $s0,$s0,$s1`；如果要多处交换，就用宏 `swap(%var1, %var2)` 打包。

**返回值**两种处理方式：① 把存放返回值的寄存器作为宏参数（不需要返回值时传 `$0`）；② 固定用 `$v0` 返回，调用后再 `move` 到别的寄存器。

**真正的函数**用跳转实现：

```asm
        li   $s0, 3
        li   $s1, 5
        move $a0, $s0        # 第 1 个参数 → $a0
        move $a1, $s1        # 第 2 个参数 → $a1
        jal  myadd           # $v0 = myadd(x, y)
        move $t0, $v0        # 取回返回值
        li   $v0, 10
        syscall              # 别忘了结束程序！

myadd:
        add  $v0, $a0, $a1   # 返回值放在 $v0
        jr   $ra
```

- `jal` 跳转的同时把**下一条指令的地址**存入 `$ra`，`jr $ra` 就能回到调用处；
- 约定用 `$a0`~`$a3` 依次传第 1~4 个参数（本课程中参数更多时可以直接从别的寄存器「偷」几个来用）；
- 返回值约定放在 `$v0`（`$v1` 亦可）。

### 递归函数与栈

递归函数**不能**照搬上面的模板。以求最大公因数的 `gcd(a, b)` 为例：

```asm
# 错误示范：递归调用会覆盖 $ra
gcd:
        beqz $a1, zero
        div  $a0, $a1
        move $a0, $a1
        mfhi $a1
        jal  gcd        # 这里的 jal 会把 $ra 覆盖成新地址
        jr   $ra        # 于是只能返回最后一次调用的位置 → 死循环
zero:
        move $v0, $a0
        jr   $ra
```

原因：**所有函数共用同一个 `$ra`**（可类比 C 里的全局变量），内层 `jal` 会覆盖外层的返回地址。

**解决办法：用栈保存 `$ra`**。栈指针是 `$sp`（初值不是 0，指向栈顶；栈向**低地址**方向生长）。

> 该课程约定下栈位于 `.data` 段末尾，`$sp` 初值为 `0x00002ffc`；而 MARS 默认内存配置下 `$sp` 初值为 `0x7fffeffc`（见前文「寄存器的初始值」）。

- **压栈**：`addi $sp, $sp, -4` + `sw $s0, 0($sp)`
- **弹栈**：`lw $s0, 0($sp)` + `addi $sp, $sp, 4`

> 注意 `sw $s0, 0($sp)` 与 `move $s0, $sp` **完全不等价**：前者是「把 `$s0` 写进 `$sp` 指向的内存」，后者是「把 `$sp` 的值复制到 `$s0`」。

**压栈 / 弹栈放在哪里**：两种写法等价——① 调用函数前压、返回后弹；② 进入函数时压、返回前弹（推荐，因为函数体只写一次，而被调用可能很多次）。

```asm
loop:
        push $ra            # 进入函数先保存返回地址
        push $t0
        push $t1
        ...
        jal  loop           # 函数内递归调用也不会破坏 $ra
        ...
        pop  $t1            # 弹出顺序必须与压栈相反（先进后出）
        pop  $t0
        pop  $ra
        jr   $ra
```

- 需要保存的不只是 `$ra`：递归中用到的任何寄存器（`$t0`、`$s0`…）都要先保存再递归；
- 非递归函数也常用栈把暂时不用的数据「寄存」起来，腾出寄存器给别处使用；
- 弹栈顺序一定要与压栈**相反**，否则程序会彻底混乱。

修正后的 `gcd`，只需在函数入口 `push $ra`、返回前 `pop $ra`：

```asm
gcd:
        push $ra
        beqz $a1, zero
        div  $a0, $a1
        move $a0, $a1
        mfhi $a1
        jal  gcd
        pop  $ra
        jr   $ra
zero:
        move $v0, $a0
        pop  $ra
        jr   $ra
```

## 程序跟踪例题

### 例 1：`bne` 循环（寄存器值跟踪）

> 运行下列代码后，`$a2` 寄存器的值是多少？其中 `li $v0, 10` 与 `syscall` 用于结束程序。

```asm
ori  $a0, $0, 1      # $a0 ← 1        计数器
ori  $a1, $0, 10     # $a1 ← 10       循环终止值
ori  $a2, $0, 20     # $a2 ← 20       被累减的寄存器
loop:                # 标签，不占指令，只代表下面这条指令的地址
addi $a0, $a0, 1     # $a0 ← $a0 + 1  计数器加 1
addi $a2, $a2, -2    # $a2 ← $a2 − 2  每轮减 2
bne  $a0, $a1, loop  # 若 $a0 ≠ $a1 则跳回 loop；相等则顺序执行
nop                  # 空操作，占位
li   $v0, 10         # $v0 ← 10（系统调用号：退出程序）
syscall
```

**① 初始化（三条 `ori`）**

| 代码 | 执行的操作 | 执行后 |
| --- | --- | --- |
| `ori $a0, $0, 1` | `$0`（恒为 0）与 1 按位或 → 1 | `$a0 = 1` |
| `ori $a1, $0, 10` | `$0` 与 10 按位或 → 10 | `$a1 = 10` |
| `ori $a2, $0, 20` | `$0` 与 20 按位或 → 20 | `$a2 = 20` |

**② 循环体逐轮执行**（每轮都是：`$a0` 加 1 → `$a2` 减 2 → 与 `$a1` 比较）

| 轮次 | `addi $a0, $a0, 1` | `addi $a2, $a2, -2` | `bne $a0, $a1, loop`（10） | 去向 |
| --- | --- | --- | --- | --- |
| 1 | 1 → **2** | 20 → **18** | 2 ≠ 10，成立 | 跳回 `loop` |
| 2 | 2 → **3** | 18 → **16** | 3 ≠ 10，成立 | 跳回 `loop` |
| 3 | 3 → **4** | 16 → **14** | 4 ≠ 10，成立 | 跳回 `loop` |
| 4 | 4 → **5** | 14 → **12** | 5 ≠ 10，成立 | 跳回 `loop` |
| 5 | 5 → **6** | 12 → **10** | 6 ≠ 10，成立 | 跳回 `loop` |
| 6 | 6 → **7** | 10 → **8** | 7 ≠ 10，成立 | 跳回 `loop` |
| 7 | 7 → **8** | 8 → **6** | 8 ≠ 10，成立 | 跳回 `loop` |
| 8 | 8 → **9** | 6 → **4** | 9 ≠ 10，成立 | 跳回 `loop` |
| 9 | 9 → **10** | 4 → **2** | 10 ≠ 10，**不成立** | 顺序往下执行 |

**③ 收尾**

| 代码 | 执行的操作 | 结果 |
| --- | --- | --- |
| `nop` | 不做任何事（也常放在分支延迟槽里） | — |
| `li $v0, 10` | 伪指令，装入常数 10 | `$v0 = 10` |
| `syscall` | 按 `$v0 = 10` 执行「退出程序」系统调用 | 程序结束，寄存器不再变化 |

**答案：`$a2 = 2`**

快速算法：循环次数 = `$a1 − $a0初值` = 10 − 1 = **9 次**，故 `$a2 = 20 − 2 × 9 = 2`。

> 易错点：`bne` 在**相等时退出**，而比较发生在每轮减 2 **之后**，所以第 9 轮的 `$a2` 仍然被减了 2（循环体共执行 9 次）。

### 例 2：`jal` / `jr` 与返回地址

> 已知第一条指令的地址为 `0x00003000`，**不考虑延迟槽**；第 4、5 条指令用于结束程序。运行后 `$a2` 寄存器的值是多少？

```asm
ori  $a0, $0, 1        # 0x00003000
jal  loop              # 0x00003004
add  $a2, $a2, $a0     # 0x00003008  ← 返回地址（$ra）指向这里
li   $v0, 10           # 0x0000300c
syscall                # 0x00003010

loop:                  # 0x00003014
sll  $a1, $a0, 3       # 0x00003014
sub  $a2, $ra, $a1     # 0x00003018
jr   $ra               # 0x0000301c
add  $a2, $a1, $a0     # 0x00003020  ← 位于 jr 之后，不会被执行
```

**① `ori` 与 `jal`**

| 代码 | 执行的操作 | 结果 |
| --- | --- | --- |
| `ori $a0, $0, 1` | `$0` 与 1 按位或 | `$a0 = 1` |
| `jal loop` | `$ra ← PC+4 = 0x00003008`，然后跳转到 `loop`（`0x00003014`） | `$ra = 0x00003008` |

> 不考虑延迟槽时，`jal` 把**下一条指令的地址**存入 `$ra`（即 `PC+4`）；若考虑延迟槽，则要存 `PC+8`。

**② 函数体（`loop` 处）**

| 代码 | 执行的操作 | 结果 |
| --- | --- | --- |
| `sll $a1, $a0, 3` | `$a1 = 1 << 3` = 8 | `$a1 = 8` |
| `sub $a2, $ra, $a1` | `$a2 = 0x00003008 − 8` | `$a2 = 0x00003000` |
| `jr $ra` | 跳转到 `$ra` = `0x00003008`，即返回 | 回到 `add $a2, $a2, $a0` |

**③ 返回后继续执行**

| 代码 | 执行的操作 | 结果 |
| --- | --- | --- |
| `add $a2, $a2, $a0` | `$a2 = 0x00003000 + 1` | `$a2 = 0x00003001` |
| `li $v0, 10` + `syscall` | 装入退出调用号并执行系统调用 | 程序结束 |

**答案：`$a2 = 0x00003001`**

**易错点**

- **`jal` 写入 `$ra` 的是返回地址**，不是跳转目标地址；`jr $ra` 正是靠它返回调用点。
- **返回地址取决于是否考虑延迟槽**：不考虑时 `$ra = PC+4 = 0x00003008`（本解）；考虑时 `$ra = PC+8 = 0x0000300c`，答案会变成 `0x00003005`。做题先看题干说明。
- **`jr $ra` 之后的那条 `add`（`0x00003020`）不会被执行**——它在返回指令之后，属于「永远不会走到」的代码。
- **`li` 是伪指令，本题按一条指令计数**（即第 4 条）；若多算一条，`loop` 及其后指令的地址会整体后移，结果就不同了。