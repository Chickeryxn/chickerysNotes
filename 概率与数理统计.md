# 确定性现象与随机现象

- **确定性现象**：在一定条件下重复进行试验，其结果总是确定的、可以事先断言的现象。
  - 例：标准大气压下，纯水加热到 \(100^\circ C\) 必然沸腾；在重力作用下，物体必然下落。
- **随机现象**：在相同条件下重复进行试验，可能结果不止一个，且每次试验前无法确定哪一个结果会出现。
  - 例：抛一枚硬币（正面或反面）、掷一颗骰子（点数）、明天的天气、测量误差。
- 随机现象在一次试验中结果不确定，但在大量重复试验中会表现出**统计规律性**，这正是概率论与数理统计研究的对象。

---

## 随机试验 **E**

> - 条件可重复：“可以在相同的条件下重复的进行。”
> - 结果预知性：每次试验的可能结果不止一个，且在试验前能明确所有可能结果。
> - 结果不确定：“每次试验前不能确定哪个结果会出现。”

## 样本

- 样本点

  > 随机试验的**每一个可能结果**称为一个样本点（基本事件），通常用小写希腊字母 \(\omega\) 表示。

- 样本空间 **S**

  > 随机试验的**所有可能结果（全部样本点）组成的集合**称为样本空间，记为 \(S\)（或 \(\Omega\)）。

- 举例：
  - 抛一枚硬币观察正反面：\(S = \{\text{正}, \text{反}\}\)，含 2 个样本点；
  - 掷一颗骰子观察点数：\(S = \{1, 2, 3, 4, 5, 6\}\)，含 6 个样本点；
  - 记录某电话交换台一分钟内接到的呼叫次数：\(S = \{0, 1, 2, \cdots\}\)（无限可列个）；
  - 测试某灯泡的寿命：\(S = \{t \mid t \ge 0\}\)（无限不可列个）。
- 分类：按样本点的多少，样本空间可分为**有限样本空间**与**无限样本空间**（后者又分无限可列与无限不可列）。

## 随机事件 **A**

- 随机事件是指在随机试验中可能发生也可能不发生的某种结果，它是样本空间的子集，通常用大写字母 \(A, B, C, \cdots\) 表示。
- 本质理解：事件 \(A\) 是样本空间 \(S\) 中若干个样本点组成的集合（\(A \subset S\)）。在一次试验中，若出现的结果属于 \(A\)，则称**事件 \(A\) 发生**。
- 由单个样本点组成的事件称为**基本事件**。

## 必然事件

> 在每次试验中**必定发生**的事件称为必然事件。它就是样本空间本身，即 \(S\)（或 \(\Omega\)），故其概率为 \(P(S) = 1\)。
>
> 例：掷骰子时"点数不超过 6"；抛硬币时"出现正面或反面"。

## 不可能事件

> 在每次试验中**必定不发生**的事件称为不可能事件。它就是空集，记作 \(\varnothing\)，其概率为 \(P(\varnothing) = 0\)。
>
> 例：掷骰子时"点数为 7"；抛硬币时"既出现正面又出现反面"。
>
> 必然事件与不可能事件虽不属于"随机"事件，但作为样本空间与空集，它们都是 \(S\) 的子集，为讨论方便仍视为事件。

## 随机事件的运算

### 随机事件的运算包括（事件即集合，运算与集合运算一一对应）

- 包含与相等：若事件 \(A\) 发生必导致事件 \(B\) 发生，则称 \(A\) 包含于 \(B\)，记 \(A \subset B\)；若 \(A \subset B\) 且 \(B \subset A\)，则称 \(A\) 与 \(B\) 相等，记 \(A = B\)。

- 和事件（并）：\(A \cup B\)，表示 \(A\)、\(B\) 中**至少有一个发生**；可推广为 \(\bigcup_{i=1}^{n} A_i\)（至少发生其中一个）。
- 积事件（交）：\(A \cap B\)（简记 \(AB\)），表示 \(A\)、\(B\) **同时发生**；可推广为 \(\bigcap_{i=1}^{n} A_i\)（全部同时发生）。
- 差事件：\(A - B = A\bar{B}\)，表示事件 \(A\) **发生而事件 \(B\) 不发生**。
- 互斥事件（不相容）：满足 \(AB = \varnothing\)，表示 \(A\)、\(B\) **不能同时发生**；若任意两个均互斥，则称 \(A_1, A_2, \cdots\) 两两互斥。
- 对立事件（逆事件）：\(A\) 的对立事件记作 \(\bar{A}\)（或 \(A^c\)），满足 \(A \cup \bar{A} = S\) 且 \(A\bar{A} = \varnothing\)，即**两个事件中必有一个发生**，且 \(\bar{A} = S - A\)。
- 补集：把事件看成集合后，在样本空间内取补集即为"事件不发生"：\(\bar{A} = S \setminus A\)。

- 运算律（事件化简的依据）：
  - 交换律：\(A \cup B = B \cup A\)，\(AB = BA\)；
  - 结合律：\(A \cup (B \cup C) = (A \cup B) \cup C\)，\(A(BC) = (AB)C\)；
  - 分配律：\(A(B \cup C) = AB \cup AC\)，\(A \cup (BC) = (A \cup B)(A \cup C)\)；
  - 德摩根律（对偶律）：\(\overline{A \cup B} = \bar{A} \cap \bar{B}\)，\(\overline{AB} = \bar{A} \cup \bar{B}\)，可推广到任意多个事件，如 \(\overline{\bigcup_{i} A_i} = \bigcap_{i} \bar{A}_i\)。

- 事件化简：
  - 利用上述运算律（尤其是德摩根律）将复杂的事件表达式化为便于计算概率的简单形式。例：\(\overline{A \cup B \cup C} = \bar{A}\,\bar{B}\,\bar{C}\)；求"至少发生一个"的对立事件时，常转化为"全不发生"。

# 概率的定义和性质

- 频率的定义

  > 在概率论与统计学中，频率（Relative Frequency）是指在相同的条件下进行了 \(n\) 次试验，某个事件 \(A\) 发生的次数 \(m\)（又称频数）与总试验次数 \(n\) 的比值

- 频率的性质（记事件 \(A\) 在 \(n\) 次试验中的频率为 \(f_n(A) = \frac{m}{n}\)）
  - 非负性：\(0 \le f_n(A) \le 1\)。
  - 规范性：必然事件的频率为 1，不可能事件的频率为 0，即 \(f_n(S) = 1\)，\(f_n(\varnothing) = 0\)。
  - 可加性：若事件 \(A\)、\(B\) 互斥，则 \(f_n(A \cup B) = f_n(A) + f_n(B)\)。

- 频率的稳定性（统计规律性）

  > 当试验次数 \(n\) 充分大时，事件 \(A\) 的频率 \(f_n(A)\) 会在某个常数 \(p\) 附近摆动，且随 \(n\) 增大摆动的幅度越来越小。这一性质称为频率的稳定性。例如抛硬币时，正面出现的频率会稳定在 0.5 附近。

- 概率的统计定义

  > 频率所稳定的那个常数 \(p\)，称为事件 \(A\) 发生的**概率**，记作 \(P(A)\)。即当 \(n\) 很大时，频率可作为概率的近似值：\(P(A) \approx \frac{m}{n}\)。
  >
  > 注意区别：**频率是试验值**，随每次试验结果而改变；**概率是理论值**，是客观存在的常数。频率是概率的近似，概率是频率的稳定中心。

## 概率的公理化定义

> - 设 \(\Omega \) 是样本空间，\(\mathcal{F}\) 是事件域。若对任一事件 \(A \in \mathscr{F}\)，定义一个实值函数 \(P(A)\)，满足：
> - 非负性：\(P(A) \ge 0\)。
> - 正则性（规范性）：\(P(\Omega) = 1\)（必然事件的概率为 1）。
> - 可列可加性：若 \(A_1, A_2, \cdots\) 互斥，则 \(P(\bigcup_{i=1}^{\infty} A_i) = \sum_{i=1}^{\infty} P(A_i)\)。

- 由公理化定义推出的基本性质
  - \(P(\varnothing) = 0\)。
  - 有限可加性：若 \(A_1, A_2, \cdots, A_n\) 两两互斥，则 \(P\left(\bigcup_{i=1}^{n} A_i\right) = \sum_{i=1}^{n} P(A_i)\)。
  - 对立事件：\(P(\bar{A}) = 1 - P(A)\)。
  - 减法公式：若 \(A \subset B\)，则 \(P(B - A) = P(B) - P(A)\)，且 \(P(A) \le P(B)\)。
  - 加法公式：\(P(A \cup B) = P(A) + P(B) - P(AB)\)（证明见下）。
  - 次可加性：\(P(A \cup B) \le P(A) + P(B)\)。

- 加法公式证明：

  证明过程要证明该公式，核心思想是将 \(A \cup B\) 拆分为若干个互不相容（互斥）的事件之和，然后利用概率的有限可加性。

  > **分解事件：**
  >
  > 将事件 \(A \cup B\) 分解为两个互不相容的部分：
  >
  > \(A\cup B=A\cup (\bar{A}B)\)
  >
  > 其中 \(A\) 与 \(\bar{A}B\) 是互不相容的（即 \(A \cap (\bar{A}B) = \emptyset\)）。
  >
  > 由概率的互斥可加性 可得：
  >
  > \(P(A\cup B)=P(A)+P(\bar{A}B)\)
  >
  > **进一步分解 \(B\)：**
  >
  > 同样地，事件 \(B\) 也可以分成两部分：
  >
  > \(B=(A\cap B)\cup (\bar{A}\cap B)=AB\cup \bar{A}B\)
  >
  > 这里的 \(AB\) 与 \(\bar{A}B\) 也是互不相容的。
  >
  > 再次应用概率的互斥可加性，有：
  >
  > \(P(B)=P(AB)+P(\bar{A}B)\)
  >
  > **代换消元：**
  >
  > 将上式变形，得到 \(\bar{A}B\) 的概率表达式：
  >
  > \(P(\bar{A}B)=P(B)-P(AB)\)
  >
  > **得出结论：**
  >
  > 把 \(P(\bar{A}B) = P(B) - P(AB)\) 代入第一步的 \(P(A \cup B) = P(A) + P(\bar{A}B)\) 中，即得：
  >
  > \(P(A\cup B)=P(A)+P(B)-P(AB)\)
  >
  > 证毕。

## 古典概型

- 定义

  > 具有以下两个特点的随机试验称为**古典概型**（又称等可能概型）：
  >
  > - **有限性**：样本空间 \(S\) 只含有有限个样本点（基本事件），基本事件总数为 \(n\)。
  > - **等可能性**：每个基本事件发生的可能性相同，即
  >   \(P(\{\omega_1\}) = P(\{\omega_2\}) = \cdots = P(\{\omega_n\}) = \frac{1}{n}\)。

- 计算公式（概率的古典定义）

  > 若试验为古典概型，事件 \(A\) 包含其中 \(k\) 个样本点，则事件 \(A\) 发生的概率为
  >
  > \[P(A) = \frac{k}{n} = \frac{事件 A 所含基本事件数}{基本事件总数} = \frac{有利于 A 的结果数}{试验的全部可能结果数}\]
  >
  > 即古典概率等于**有利结果数与等可能结果总数之比**，它是公理化定义在"有限等可能"样本空间下的特例。

- 基本性质
  - 由公式 \(P(A) = \frac{k}{n}\) 定义的古典概率满足概率公理化定义的全部性质（见上文"公理化定义推出的基本性质"），其中常用的有：
  - 对立事件：\(P(\bar{A}) = 1 - P(A)\)，常用于"至少一次"型问题的转化；
  - 加法公式：\(P(A \cup B) = P(A) + P(B) - P(AB)\)；
  - 若 \(A \subset B\)，则 \(P(A) \le P(B)\)。

- 计数基础（排列与组合）
  - 加法原理：完成一件事有若干类互斥方式，则方法总数为各类方法数之和。
  - 乘法原理：完成一件事需依次经过若干步骤，则方法总数为各步方法数之积。
  - 排列：从 \(n\) 个不同元素中**按次序**取出 \(m\) 个（不放回），排法数为
    \(A_n^m = n(n-1) \cdots (n-m+1) = \frac{n!}{(n-m)!}\)；全排列 \(A_n^n = n!\)。
  - 组合：从 \(n\) 个不同元素中**不计次序**取出 \(m\) 个，取法数为
    \(C_n^m = \binom{n}{m} = \frac{n!}{m! (n-m)!} = \frac{A_n^m}{m!}\)，且有 \(C_n^m = C_n^{n-m}\)。
  - 计算步骤：① 弄清试验的样本空间并数出等可能基本事件总数 \(n\)；② 用排列组合数出事件 \(A\) 所含基本事件数 \(k\)；③ 代入 \(P(A) = k/n\)。

- 典型模型与例题
  - **不放回抽球**：袋中有 \(a\) 个白球、\(b\) 个黑球，一次任取 \(k\) 个，恰含 \(r\) 个白球的概率为
    \(P = \frac{C_a^r C_b^{k-r}}{C_{a+b}^{k}}\)（超几何分布背景）。
  - **有放回抽球**：每次取出后放回、共抽 \(k\) 次，恰有 \(r\) 次取到白球的概率为
    \(P = C_k^r \left(\frac{a}{a+b}\right)^r \left(\frac{b}{a+b}\right)^{k-r}\)（二项分布背景）。
  - **分房问题**：把 \(n\) 个人等可能地分配到 \(N\) 个房间（\(N \ge n\)），每间至多一人的概率为 \(\frac{A_N^n}{N^n}\)。
  - **生日问题**：\(n\) 个人生日互不相同的概率为 \(\frac{A_{365}^n}{365^n}\)，故至少两人生日相同的概率为
    \(1 - \frac{A_{365}^n}{365^n}\)（当 \(n = 23\) 时该概率已超过 0.5）。

## 几何概型

- 定义

  > 若随机试验满足以下两个条件，则称为**几何概型**：
  >
  > - **样本空间可度量**：样本空间 \(S\) 是一个可度量的几何区域（如直线上的区间、平面上的区域、空间中的立体等），含有无限多个样本点，其"度量"可取长度、面积或体积。
  > - **等可能性（均匀性）**：试验结果等可能地落入 \(S\) 中的任一点，即样本点落在任一子区域 \(A\) 内的概率只与 \(A\) 的度量成正比，与 \(A\) 的位置和形状无关。

- 计算公式

  > 对几何概型，事件 \(A\) 发生的概率为
  >
  > \[P(A) = \frac{S_A 的度量}{S 的度量}\]
  >
  > 其中"度量"依样本空间而定：**一维取长度，二维取面积，三维取体积**。该式可看作古典概型向"无限多个等可能结果"情形的推广。

- 基本性质
  - 仍满足概率的公理化定义：非负性 \(P(A) \ge 0\)、规范性 \(P(S) = 1\)、可列可加性。
  - 重要区别（几何概型特有）：
    - **概率为 0 的事件不一定是不可能事件**。例如点落在区域中某单个点上的概率为 0，但该点仍有可能被选中。
    - **概率为 1 的事件不一定是必然事件**。例如从区域 \(S\) 中去掉有限个点后，对应事件概率仍为 1，但它并非 \(S\) 的全部。
  - 与古典概型不同：几何概型的样本点无限多，不能靠"个数之比"\(k/n\) 计数，而必须用**测度（长度/面积/体积）之比**来计算。

- 计算方法
  - 解题步骤：
    1. 明确样本空间，选取适当的坐标（或变量），把它表示为可度量的几何区域 \(S\)，并求其度量；
    2. 将事件 \(A\) 转化为关于坐标的约束条件（不等式组），作出"有利区域"并求其度量；
    3. 代入 \(P(A) = \frac{S_A 的度量}{S 的度量}\) 计算。注意：仅当点等可能落入时才能直接套用。
  - 典型例题：
    - **随机取点（长度比）**：在区间 \([0, 1]\) 上随机取一点 \(X\)，则 \(P(a \le X \le b) = b - a\)。
    - **会面问题（面积比）**：甲乙两人约定在时段 \([0, T]\) 内到某地会面，先到者最多等待 \(t\)（\(0 < t < T\)）便会离开。设两人到达时刻分别为 \(x, y\)，则能会面当且仅当 \(|x - y| \le t\)。样本空间为正方形 \(0 \le x, y \le T\)（面积 \(T^2\)），有利区域面积为 \(T^2 - (T - t)^2 = 2Tt - t^2\)，故
      \(P = \frac{2Tt - t^2}{T^2} = 1 - \left(1 - \frac{t}{T}\right)^2\)。
    - **蒲丰投针问题（拓展）**：在间距为 \(a\) 的平行线间随机投掷长为 \(l\)（\(l < a\)）的针，针与平行线相交的概率为 \(P = \frac{2l}{\pi a}\)，可用于估计圆周率 \(\pi\)。

## 条件概率和贝叶斯公式

### 条件概率的定义

> 设 \(A\)、\(B\) 是两个事件，且 \(P(A) > 0\)，则称
>
> \[P(B \mid A) = \frac{P(AB)}{P(A)}\]
>
> 为在事件 \(A\) **已经发生**的条件下，事件 \(B\) 发生的**条件概率**。
>
> 其中 \(P(AB)\) 是 \(A\) 与 \(B\) 同时发生的（无条件）概率，\(P(A)\) 作为分母起"归一化"作用：把样本空间 \(S\) 压缩为 \(A\)，条件概率相当于在缩小后的样本空间 \(A\) 上重新分配概率。

- 对定义的理解：**条件概率仍是概率**

  > 固定事件 \(A\)（\(P(A) > 0\)），把 \(P(B \mid A)\) 看成 \(B\) 的函数，则它同样满足概率的三条公理，即 \(\left(\Omega, \mathscr{F}, P(\cdot \mid A)\right)\) 仍是一个概率空间：
  >
  > 1. **非负性**：对任一事件 \(B\)，\(P(B \mid A) = \dfrac{P(AB)}{P(A)} \ge 0\)。
  > 2. **规范性**：\(P(\Omega \mid A) = \dfrac{P(\Omega A)}{P(A)} = \dfrac{P(A)}{P(A)} = 1\)（条件概率下的"必然事件"是 \(A\) 本身，即 \(P(A \mid A) = 1\)）。
  > 3. **可列可加性**：若 \(B_1, B_2, \cdots\) 两两互斥，则
  >    \(P\left(\bigcup_{i=1}^{\infty} B_i \,\middle|\, A\right) = \sum_{i=1}^{\infty} P(B_i \mid A)\)。
  >
  > 正因为满足这三条，前文由公理推出的性质对 \(P(\cdot \mid A)\) 全部成立，如
  >
  > \(P(\bar{B} \mid A) = 1 - P(B \mid A)\)，\(P(B \cup C \mid A) = P(B \mid A) + P(C \mid A) - P(BC \mid A)\)。

- 由定义立得的推论
  - **乘法公式**：若 \(P(A) > 0\)，则 \(P(AB) = P(A) P(B \mid A)\)；若 \(P(B) > 0\)，亦有 \(P(AB) = P(B) P(A \mid B)\)。
  - 推广至多个事件：当 \(P(A_1 A_2 \cdots A_{n-1}) > 0\) 时，
    \(P(A_1 A_2 \cdots A_n) = P(A_1) P(A_2 \mid A_1) P(A_3 \mid A_1 A_2) \cdots P(A_n \mid A_1 A_2 \cdots A_{n-1})\)。
  - 常把 \(P(B \mid A) = 1 - P(\bar{B} \mid A)\) 与乘法公式配合，用于"至少一次"型条件概率的计算。

### 全概率公式

- 完备事件组（样本空间的一个划分）

  > 设 \(B_1, B_2, \cdots, B_n\) 是一组事件，若满足
  >
  > 1. **互斥**：\(B_i B_j = \varnothing\)（\(i \ne j\)），即任意两个不能同时发生；
  > 2. **完备**：\(B_1 \cup B_2 \cup \cdots \cup B_n = \Omega\)，即试验中必有一个发生；
  > 3. **概率非零**：\(P(B_i) > 0\)（\(i = 1, 2, \cdots, n\)）。
  >
  > 则称 \(B_1, B_2, \cdots, B_n\) 为样本空间 \(\Omega\) 的一个**完备事件组**（或称**划分**）。
  >
  > 直观理解：把样本空间"块块切开、既不重叠也不遗漏"，试验结果必落入且只落入其中一块。

- 全概率公式

  > 设 \(B_1, B_2, \cdots, B_n\) 为完备事件组，\(A\) 为任一事件，则
  >
  > \[P(A) = \sum_{i=1}^{n} P(B_i) P(A \mid B_i)\]
  >
  > 也称为**全概率公式**；若划分可列（\(i = 1, 2, \cdots\)），则相应地有 \(P(A) = \displaystyle\sum_{i=1}^{\infty} P(B_i) P(A \mid B_i)\)。

- 公式的推导（用乘法公式 + 可加性）

  > 1. 分解事件：由 \(\Omega = B_1 \cup B_2 \cup \cdots \cup B_n\) 得
  >    \(A = A\Omega = A(B_1 \cup B_2 \cup \cdots \cup B_n) = AB_1 \cup AB_2 \cup \cdots \cup AB_n\)。
  > 2. 因 \(B_i\) 两两互斥，故 \(AB_i\) 也两两互斥，由有限可加性得
  >    \(P(A) = P(AB_1) + P(AB_2) + \cdots + P(AB_n)\)。
  > 3. 对每一项用乘法公式 \(P(AB_i) = P(B_i) P(A \mid B_i)\)，即得 \(P(A) = \displaystyle\sum_{i=1}^{n} P(B_i) P(A \mid B_i)\)。

- 使用要点

  > 全概率公式的作用是**把复杂事件 \(A\) 的概率"分摊"到若干种情形上再求和**：当 \(A\) 直接计算困难，而它对各类原因 \(B_i\) 的条件概率 \(P(A \mid B_i)\) 容易求得时，就用它。

  - 关键步骤：
    1. 找出（或构造）导致 \(A\) 发生的完备事件组 \(B_1, \cdots, B_n\)，通常取"试验的两个阶段"中**第一阶段的所有可能结果**；
    2. 求出各原因的概率 \(P(B_i)\)（常为已知或易算）；
    3. 求出各原因下 \(A\) 发生的条件概率 \(P(A \mid B_i)\)；
    4. 代入公式加权求和。
  - 常见划分来源：产品的"合格/不合格"、机器的"正常/故障"、抽球的"先抽到白/黑"、传染病的"患病/未患病"等。
  - **与贝叶斯公式的关系：全概率公式是"由原因推结果"（求 \(P(A)\)），贝叶斯公式则是"由结果反推原因"（求 \(P(B_i \mid A)\)），后者正是以前者的各项 \(P(B_j)P(A\mid B_j)\) 作分母的归一化。**
  - 特例：取 \(B_1 = B\)、\(B_2 = \bar{B}\)（\(0 < P(B) < 1\)），则 \(P(A) = P(B)P(A \mid B) + P(\bar{B})P(A \mid \bar{B})\)，这是最常用的两分支形式。

- 举例

  > **例**：某厂有甲、乙两条生产线，产量分别占 \(60\%\)、\(40\%\)，次品率分别为 \(2\%\)、\(5\%\)。随机取一件产品，求它是次品的概率。
  >
  > 解：记 \(A = \)"取到次品"，\(B_1 = \)"产品来自甲线"，\(B_2 = \)"产品来自乙线"，则 \(B_1, B_2\) 构成完备事件组，且
  > \(P(B_1) = 0.6\)，\(P(B_2) = 0.4\)，\(P(A \mid B_1) = 0.02\)，\(P(A \mid B_2) = 0.05\)。
  > 由全概率公式
  > \[P(A) = P(B_1)P(A \mid B_1) + P(B_2)P(A \mid B_2) = 0.6 \times 0.02 + 0.4 \times 0.05 = 0.032\]
  > 即任取一件为次品的概率为 \(3.2\%\)。

- 与乘法公式的区别（易混点）

  - 乘法公式求的是**积事件** \(AB\)（或交事件）的概率，形如 \(P(A) P(B \mid A)\)，式子中仍是"条件概率"；
  - 全概率公式求的是**单一事件** \(A\) 的概率，它已把条件概率按 \(P(B_i)\) 加权"平均"掉，结果是**无条件概率**。

### 贝叶斯公式

- 公式（又称**逆概率公式**）

  > 设 \(B_1, B_2, \cdots, B_n\) 为完备事件组，\(A\) 为任一事件且 \(P(A) > 0\)，则
  >
  > \[P(B_i \mid A) = \frac{P(B_i) P(A \mid B_i)}{\displaystyle\sum_{j=1}^{n} P(B_j) P(A \mid B_j)} \qquad (i = 1, 2, \cdots, n)\]
  >
  > 分母正是全概率公式给出的 \(P(A)\)，因此公式也常写成
  >
  > \[P(B_i \mid A) = \frac{P(AB_i)}{P(A)} = \frac{P(B_i) P(A \mid B_i)}{P(A)}\]

- 公式的推导

  > 1. 由条件概率的定义：\(P(B_i \mid A) = \dfrac{P(AB_i)}{P(A)}\)；
  > 2. 分子用乘法公式：\(P(AB_i) = P(B_i) P(A \mid B_i)\)；
  > 3. 分母用全概率公式：\(P(A) = \displaystyle\sum_{j=1}^{n} P(B_j) P(A \mid B_j)\)。
  >
  > 三步代入即得贝叶斯公式。

- 三个术语（理解公式的关键）

  - **先验概率**：\(P(B_i)\)，即试验（观察到 \(A\)）**之前**对原因 \(B_i\) 发生可能性的认识，来自以往经验或已知资料。
  - **后验概率**：\(P(B_i \mid A)\)，即**已知结果 \(A\) 发生之后**，重新修正后对原因 \(B_i\) 的判断，公式的作用就是把先验概率更新为后验概率。
  - **似然（条件概率）**：\(P(A \mid B_i)\)，表示若原因 \(B_i\) 成立，则结果 \(A\) 出现的可能性；它在各原因间的相对大小决定了对先验概率的"加权修正"方向。
  - 直观表述：**后验概率 \(\propto\) 先验概率 \(\times\) 似然**，即 \(P(B_i \mid A) \propto P(B_i) P(A \mid B_i)\)，分母 \(\sum_j P(B_j)P(A\mid B_j)\) 只是起归一化（使各后验概率之和为 1）的作用。

#### 先验概率与后验概率的含义

- **先验概率** \(P(B_i)\)

  > "先"是相对**观察结果 \(A\) 之前**而言的：它是在获得新的试验信息之前，对原因 \(B_i\) 出现的可能性所作出的判断。
  >
  > - **来源**：以往的经验、历史统计资料、产品的设计指标，或在没有信息时按"等可能"作假定（如取 \(P(B_i) = 1/n\)）。
  > - **特点**：与本次试验结果无关，在试验**之前**就已确定；不同的信息来源可能给出不同的先验概率。
  > - **例**：上例中"甲线产量占 \(60\%"是长期生产数据，取产品之前就知道，故 \(P(B_1) = 0.6\) 就是先验概率。

- **后验概率** \(P(B_i \mid A)\)

  > "后"是相对**观察到结果 \(A\) 之后**而言的：它是在已知"结果 \(A\) 发生了"这一新信息后，对原因 \(B_i\) 重新修正过的判断。
  >
  > - **来源**：由先验概率 \(P(B_i)\) 与似然 \(P(A \mid B_i)\) 通过贝叶斯公式算出，不是凭空给定的。
  > - **特点**：**依赖于所观察到的结果**——同一组先验概率，若观察到的是不同的 \(A\)，得到的后验概率也不同；且各原因的后验概率之和为 1。
  > - **例**：上例中"发现这件产品是次品"之后，甲线的可能性由 \(0.6\) 修正为 \(0.375\)，这就是后验概率。

- 二者的关系与"更新"过程

  > $$\text{先验概率} \xrightarrow[\text{贝叶斯公式}]{\text{加入证据 } A} \text{后验概率}$$
  >
  > - 贝叶斯公式的实质是一次**信息更新**：以先验概率为起点，用似然的相对大小作加权修正，再归一化即得后验概率。
  >
  > | 名称 | 记号 | 出现时机 | 作用 |
  > | --- | --- | --- | --- |
  > | 先验概率 | \(P(B_i)\) | 观察 \(A\) 之前 | 提供原始判断（起点） |
  > | 似然 | \(P(A \mid B_i)\) | 观察 \(A\) 之中 | 提供证据对各原因的"支持力度" |
  > | 后验概率 | \(P(B_i \mid A)\) | 观察 \(A\) 之后 | 更新后的判断（终点） |
  >
  > - **可继续迭代**：若又获得新的证据 \(A'\)，可把当前的后验概率 \(P(B_i \mid A)\) 当作新的先验概率，再用贝叶斯公式继续修正，得到 \(P(B_i \mid A A')\)——这正是贝叶斯方法"随证据不断学习"的思想。
  > - **数值感受**：先验概率大不一定后验概率大。上例中 \(P(B_1) = 0.6 > P(B_2) = 0.4\)，但 \(P(A \mid B_1) = 0.02 < P(A \mid B_2) = 0.05\) *（甲线更不容易出次品）*，结果 \(P(B_1 \mid A) = 0.375 < P(B_2 \mid A) = 0.625\)，判断发生了"反转"。

- 使用要点

  - 关键步骤：
    1. 先按全概率公式的流程确定完备事件组 \(B_1, \cdots, B_n\) 与事件 \(A\)，并算出 \(P(A)\)；
    2. 对关心的那个原因 \(B_i\)，求出分子 \(P(B_i) P(A \mid B_i)\)；
    3. 用 \(\dfrac{分子}{P(A)}\) 得到后验概率；若要求多个原因的后验概率，注意它们的和应为 1，可用于验算。
  - 常用两分支形式：当 \(B_1 = B\)、\(B_2 = \bar{B}\) 时，
    \[P(B \mid A) = \frac{P(B) P(A \mid B)}{P(B) P(A \mid B) + P(\bar{B}) P(A \mid \bar{B})}\]
  - 与全概率公式的区别：全概率公式是**"由原因推结果"**，用各类原因的已知信息算出结果 \(A\) 的无条件概率；贝叶斯公式是**"由结果反推原因"**，用观察到的结果 \(A\) 去修正对原因的判断。两者一正一反，常配合使用。
  - 特别注意：分子中 \(P(B_i)\) 与 \(P(A \mid B_i)\) 的顺序不能写反，贝叶斯公式中**结果 \(A\) 始终出现在条件符号右边**。

- 举例（承接上例）

  > **例**：某厂有甲、乙两条生产线，产量分别占 \(60\%\)、\(40\%\)，次品率分别为 \(2\%\)、\(5\%\)。现从产品中任取一件，发现它是次品，求它来自甲线的概率。
  >
  > 解：记 \(A = \)"取到次品"，\(B_1 = \)"产品来自甲线"，\(B_2 = \)"产品来自乙线"。由上例已知 \(P(A) = 0.032\)，且
  > \(P(B_1) = 0.6\)，\(P(A \mid B_1) = 0.02\)，\(P(B_2) = 0.4\)，\(P(A \mid B_2) = 0.05\)。
  > 由贝叶斯公式
  > \[P(B_1 \mid A) = \frac{P(B_1) P(A \mid B_1)}{P(A)} = \frac{0.6 \times 0.02}{0.032} = 0.375\]
  > 同理 \(P(B_2 \mid A) = \dfrac{0.4 \times 0.05}{0.032} = 0.625\)，二者之和为 1，可作验算。
  > 结论：该次品来自甲线的概率为 \(37.5\%\)。注意：甲线产量虽占 \(60\%\)（先验概率大），但因其次品率低，在"已知是次品"的条件下，来自甲线的可能性反而降为 \(37.5\%\)（后验概率小）——这正是贝叶斯公式"用结果修正判断"的体现。

## 事件的独立性

### 两个事件的独立性

- 定义

  > 设 \(A\)、\(B\) 是两个事件，若
  >
  > \[P(AB) = P(A) P(B)\]
  >
  > 则称事件 \(A\) 与 \(B\) **相互独立**，简称 \(A\)、\(B\) 独立。
  >
  > 直观含义：\(A\) 发生与否对 \(B\) 发生的可能性没有影响（反之亦然），此时乘法公式中的条件概率退化为无条件概率。

- 定义的等价形式（当 \(P(A) > 0\)、\(P(B) > 0\) 时）

  > \[A \text{ 与 } B \text{ 独立} \iff P(B \mid A) = P(B) \iff P(A \mid B) = P(A) \iff P(AB) = P(A)P(B)\]
  >
  > 说明：定义式 \(P(AB)=P(A)P(B)\) 不需要任何概率大于零的条件，且对 \(P(A)=0\) 或 \(P(B)=0\) 的情形依然适用，故通常以它作为统一的定义；而条件概率的形式只便于直观理解。

- 为什么这样规定是合理的

  > **把直观翻译成条件概率。** "\(A\) 发生与否不影响 \(B\)"的最自然表述是：在已知 \(A\) 发生的条件下，\(B\) 的概率与不知道 \(A\) 时一样，即（当 \(P(A) > 0\) 时）
  >
  > \[P(B \mid A) = P(B)\]
  >
  > 同理也应有 \(P(A \mid B) = P(A)\)，两条式子反映的是同一件事。
  >
  > 1. **不需要附加条件**。\(P(B \mid A)\) 要求 \(P(A) > 0\)，否则无定义；而 \(P(AB) = P(A)P(B)\) 对 \(P(A) = 0\) 或 \(P(A) = 1\) 依然有意义，从而使"概率为 0 的事件与任何事件独立"这类结论自然成立（若沿用条件形式，这些情形只能靠额外约定来补）。
  > 2. **关于 \(A\)、\(B\) 对称**。"相互独立"本应是对称关系，乘积式把两者放在同等地位；而 \(P(B \mid A)\) 天然带方向，还需另证 \(P(A \mid B) = P(A)\) 才显对称。
  > 3. **能自然推广到多个事件**。写成乘积式后，多事件只需要求"任意一部分事件的概率之积等于积事件的概率"，"两两独立"与"相互独立"的区别才有清晰的表述（见下节）。
  > 4. **计算上最省力**。乘法公式 \(P(AB) = P(A)P(B\mid A)\) 在独立时退化为直接相乘 \(P(AB) = P(A)P(B)\)；\(n\) 次独立重复试验的概率也随之化为一串因式的连乘，成为伯努利概型的基础。
  >
  > **反向检验：换一个式子行不行？** 例如取"互斥" \(P(AB) = 0\) 显然不行——那样 \(A\) 发生反而使 \(B\) 必然不发生，与"互不影响"正相反；取 \(P(A \mid B) = P(A \mid \bar{B})\) 虽也表达"不影响"，但同样绕不开 \(P(B) \in (0,1)\) 的限制。
  >
  > 乘积形式是**同时满足上述四条要求的最简形式**，而且在 \(P(A)P(B) > 0\) 时它是唯一与"条件不起作用"等价的代数式，因此用它作定义是合理且必然的选择。

- 基本性质

  - 必然事件 \(\Omega\) 与不可能事件 \(\varnothing\) 与任何事件相互独立：\(P(A\Omega) = P(A) = P(A) \cdot 1\)。
  - 若 \(P(A) = 0\) 或 \(P(A) = 1\)，则 \(A\) 与任何事件 \(B\) 独立。
  - **对偶性**：若 \(A\) 与 \(B\) 独立，则 \(A\) 与 \(\bar{B}\)、\(\bar{A}\) 与 \(B\)、\(\bar{A}\) 与 \(\bar{B}\) 也都相互独立（求 \(P(\bar{A}\bar{B})\) 时可放心拆开相乘）。

- 独立性必须由定义**验证**，不能凭"事件看起来无关"想当然地断定

  > 原因有三：
  >
  > 1. **独立性是概率等式，不是对事件内容的描述**。定义 \(P(AB) = P(A)P(B)\) 比较的是三个数值，与两个事件在文字上是否相关无关；只有算完 \(P(AB)\) 并与 \(P(A)P(B)\) 对比，才能下结论。
  > 2. **同一对事件在不同试验方式下结论可能不同**。如"第一次取到白球"与"第二次取到白球"：**有放回**时独立，**不放回**时不独立——事件本身没变，变的是概率分布。
  > 3. **直观印象常出错**，两种方向都有反例：
  >    - *看着无关，实则相关*：掷一颗骰子，\(A = \{3, 4, 5, 6\}\)（点数大于 2）、\(B = \{1, 2, 3, 4\}\)（点数不超过 4）。二者含义上似乎毫不相干，但
  >      \(P(A) = \dfrac{2}{3}\)，\(P(B) = \dfrac{2}{3}\)，\(P(AB) = P(\{3,4\}) = \dfrac{1}{3} \ne \dfrac{4}{9} = P(A)P(B)\)，故 \(A\)、\(B\) **不独立**。
  >    - *看着有关，实则独立*：掷一颗骰子，\(A = \{2, 4, 6\}\)（偶数）、\(B = \{1, 2, 3, 4, 5\}\)（点数少于 6）。点数为偶数与否似乎会影响"是否小于 6"，但
  >      \(P(A) = \dfrac{1}{2}\)，\(P(B) = \dfrac{5}{6}\)，\(P(AB) = P(\{2,4\}) = \dfrac{1}{3} = \dfrac{1}{2} \times \dfrac{5}{6}\)，故 \(A\)、\(B\) **独立**。
  >
  > 所以判断独立性只有一个可靠办法：**回到定义做计算**。

- 独立与互斥的区别（最重要的易混点）

  > | | 互斥（\(AB = \varnothing\)） | 独立（\(P(AB) = P(A)P(B)\)） |
  > | --- | --- | --- |
  > | 性质类型 | 事件间的**集合关系** | 事件间的**概率关系** |
  > | 含义 | 不能同时发生 | 一个发生不影响另一个发生的概率 |
  > | 关系 | 若 \(P(A) > 0\)、\(P(B) > 0\)，则**互斥必不独立**：此时 \(P(AB) = 0 \ne P(A)P(B) > 0\) | 独立时两事件一般可以同时发生 |
  >
  > 记忆：**互斥是"不能一起发生"，独立是"一起发生也没关系"**，二者没有蕴含关系，只是在 \(P(A)P(B)>0\) 时不能共存。

### 多个事件的独立性

- 两两独立与相互独立

  > 设 \(A_1, A_2, \cdots, A_n\)（\(n \ge 2\)）为一组事件。
  >
  > - **两两独立**：若其中任意两个事件都独立，即 \(P(A_i A_j) = P(A_i)P(A_j)\)（\(1 \le i < j \le n\)），则称它们**两两独立**。
  > - **相互独立**：若对任意 \(k\)（\(2 \le k \le n\)）及任意 \(k\) 个不同的事件 \(A_{i_1}, \cdots, A_{i_k}\)，都有
  >   \[P(A_{i_1} A_{i_2} \cdots A_{i_k}) = P(A_{i_1}) P(A_{i_2}) \cdots P(A_{i_k})\]
  >   则称它们**相互独立**，简称 \(A_1, \cdots, A_n\) 独立。
  >
  > 注意：相互独立要求 \(\dbinom{n}{2} + \dbinom{n}{3} + \cdots + \dbinom{n}{n} = 2^n - n - 1\) 个等式同时成立，即"任意一部分事件的乘积都可拆开相乘"。

- 两者的关系：**相互独立 \(\Rightarrow\) 两两独立，反之不成立**

  > **反例（伯恩斯坦例）**：取样本空间 \(\Omega = \{\omega_1, \omega_2, \omega_3, \omega_4\}\)，四个样本点等可能（各 \(1/4\)）。令
  > \(A = \{\omega_1, \omega_2\}\)，\(B = \{\omega_1, \omega_3\}\)，\(C = \{\omega_1, \omega_4\}\)，
  > 则 \(P(A) = P(B) = P(C) = \dfrac{1}{2}\)，而
  > \(P(AB) = P(AC) = P(BC) = \dfrac{1}{4} = \dfrac{1}{2} \times \dfrac{1}{2}\)，即 \(A, B, C\) **两两独立**；
  > 但 \(P(ABC) = P(\{\omega_1\}) = \dfrac{1}{4} \ne P(A)P(B)P(C) = \dfrac{1}{8}\)，故 \(A, B, C\) **不相互独立**。
  >
  > 结论：检验多个事件的独立性时，除两两之间的等式外，**还必须检验所有"三个及以上"的组合等式**。

- 基本性质

  - 若 \(A_1, \cdots, A_n\) 相互独立，则其中任意 \(m\)（\(2 \le m \le n\)）个事件也相互独立（"整体独立必有部分独立"）。
  - 若 \(A_1, \cdots, A_n\) 相互独立，则把其中任意一部分事件换成它们的对立事件后，所得的一组事件仍相互独立。例如 \(A_1, \bar{A_2}, \cdots, \bar{A_n}\) 相互独立。
  - 若 \(A_1, \cdots, A_n\) 相互独立，则乘积事件的概率可直接展开：\(P(A_1 A_2 \cdots A_n) = \prod_{i=1}^{n} P(A_i)\)。
  - **"至少有一个发生"的常用公式**：\(A_1, \cdots, A_n\) 相互独立时，
    \[P\left(\bigcup_{i=1}^{n} A_i\right) = 1 - P(\bar{A}_1 \bar{A}_2 \cdots \bar{A}_n) = 1 - \prod_{i=1}^{n}\left[1 - P(A_i)\right]\]
    这是把"至少一次"转化为"全不发生"再用独立性拆开相乘的典型技巧。

- 举例

  > **例 1（独立性的验证）**：袋中有 4 个球（3 白 1 黑），有放回地连取两次，每次取一球。记 \(A = \)"第一次取到白球"，\(B = \)"第二次取到白球"。因每次取球后放回，两次结果互不影响，且
  > \(P(A) = P(B) = \dfrac{3}{4}\)，\(P(AB) = \dfrac{3}{4} \times \dfrac{3}{4} = \dfrac{9}{16} = P(A)P(B)\)，故 \(A\)、\(B\) 独立。
  > 若改为**不放回**抽取，则 \(P(B \mid A) = \dfrac{2}{3} \ne \dfrac{3}{4} = P(B)\)，此时 \(A\)、\(B\) **不独立**。
  >
  > **例 2（至少一次）**：三人独立地破译一份密码，各自能译出的概率分别为 \(\dfrac{1}{5}\)、\(\dfrac{1}{3}\)、\(\dfrac{1}{4}\)，求密码被译出的概率。
  > 解：记 \(A_i = \)"第 \(i\) 人译出"，则 \(A_1, A_2, A_3\) 相互独立，密码被译出即"至少一人译出"：
  > \[P(A_1 \cup A_2 \cup A_3) = 1 - P(\bar{A}_1)P(\bar{A}_2)P(\bar{A}_3) = 1 - \frac{4}{5} \times \frac{2}{3} \times \frac{3}{4} = 1 - \frac{2}{5} = \frac{3}{5}\]
  >
  > **例 3（系统可靠性）**：设各元件独立工作，元件 \(i\) 的正常工作概率为 \(p_i\)（可靠度）。
  > - **串联系统**（全部正常才工作）：\(P = \prod_{i=1}^{n} p_i\)，可靠度随元件数增加而下降；
  > - **并联系统**（至少一个正常即工作）：\(P = 1 - \prod_{i=1}^{n}(1 - p_i)\)，可靠度随元件数增加而提高。

- 与前文的联系

  - 独立时**乘法公式简化为** \(P(AB) = P(A)P(B)\)，无需再求条件概率；反之，若已知 \(P(AB) \ne P(A)P(B)\)，则两事件必不独立。
  - 独立条件下把"多次重复试验"的概率化为一串因式连乘，正是下一节**伯努利概型**（\(n\) 重独立重复试验）的基础。